
## 2026-04-06 - US-001 (chat-pipeline-fixes)
- Wired ConversationContextCache into both http-bridge.ts and ws-bridge.ts
- On context rebuild: check cache first (skip if prologFacts provided), use cached systemPrompt/characterName if available
- On cache miss: build context via buildContext(), then cache the result
- After each exchange: append user and assistant messages to cache via conversationContextCache.append()
- Added cache hit/miss logging in both bridges
- Added test: verifies cache is empty initially, populated after first message, and grows after second message
- Files changed: server/services/conversation/http-bridge.ts, server/services/conversation/ws-bridge.ts, server/__tests__/ws-bridge.test.ts
- **Learnings for future iterations:**
  - ConversationContextCache singleton is exported as `conversationContextCache` — import from `conversation-context-cache.js`
  - Cache key format: `ConversationContextCache.chatKey(worldId, characterId, playerId)` → `chat:{worldId}:{characterId}:{playerId}`
  - When prologFacts are provided, skip cache (they change per turn and invalidate context)
  - Pre-existing type errors in client/ are expected — check only for errors in modified files
---

## 2026-04-06 - US-002 (chat-pipeline-fixes)
- Added Prolog-first routing to http-bridge.ts and ws-bridge.ts
- Added greeting/farewell classification using regex patterns (supports English, French, Spanish, German)
- Before LLM call: classify message → if greeting/farewell, call prologLLMRouter.tryPrologFirst() → if answered with confidence >= 0.6, send template response directly (with TTS), skip LLM
- Complex messages fall through to LLM as before
- Added 3 tests: complex message falls through, greeting intercepted by Prolog, existing test updated to use non-greeting
- Files changed: server/services/conversation/http-bridge.ts, server/services/conversation/ws-bridge.ts, server/__tests__/ws-bridge.test.ts
- **Learnings for future iterations:**
  - PrologLLMRouter.tryPrologFirst() greeting/farewell cases return templates without needing a Prolog engine — they work in test env
  - The `prologAutoSync.getEngine(worldId)` call throws when no engine exists; greeting/farewell cases catch this and still return templates
  - When adding Prolog-first routing, existing tests that send greeting-like messages (e.g. "Hello NPC!") will be intercepted — update test inputs accordingly
---

## 2026-04-06 - US-003 (chat-pipeline-fixes)
- Added `prologFacts` field to `SendTextOptions` in packages/typescript types
- Updated `IChatProvider` interface to accept prologFacts as 3rd parameter
- Threaded prologFacts through InsimulClient → ServerChatProvider → sendTextSSE/sendTextWS
- Added `setPrologFactsProvider()` to BabylonChatPanel; wired from BabylonGame using GamePrologEngine.getAllFacts()
- Added `getAllFacts()` public method to GamePrologEngine (delegates to TauPrologEngine)
- Updated ws-bridge.ts to destructure prologFacts from textInput and pass to buildContext()
- Cap prologFacts at 50 entries in ServerChatProvider to avoid bloating requests
- Files changed: packages/typescript/src/{types.ts,client.ts}, packages/typescript/src/providers/chat/{types.ts,server-chat-provider.ts}, shared/game-engine/{rendering/BabylonChatPanel.ts,rendering/BabylonGame.ts,logic/GamePrologEngine.ts}, server/services/conversation/{http-bridge.ts,ws-bridge.ts}, server/__tests__/ws-bridge.test.ts
- **Learnings for future iterations:**
  - Engine drift hook blocks commits when game-system files change — run `npm run drift:update` to update hashes
  - buildContext() hangs in test env (no MongoDB) — tests must avoid triggering rebuild with non-empty prologFacts
  - Use `prologFacts?.length > 0` not `prologFacts` for truthiness checks (empty array is truthy)
---

## 2026-04-06 - US-004 (chat-pipeline-fixes)
- Added localized greeting/farewell templates to PrologLLMRouter for French, Spanish, German
- Templates stored as Record<string, string[]> keyed by language code (en/fr/es/de)
- Added targetLanguage parameter to tryPrologFirst() — greeting/farewell cases use language-aware template selection
- Helper functions getGreetingTemplates()/getFarewellTemplates() with English fallback
- Passed languageCode from http-bridge and ws-bridge to tryPrologFirst()
- Added comprehensive test suite: 9 tests covering all languages, fallback, and backwards compatibility
- Files changed: server/services/prolog-llm-router.ts, server/services/conversation/{http-bridge.ts,ws-bridge.ts}, server/__tests__/prolog-llm-router.test.ts
- **Learnings for future iterations:**
  - PrologLLMRouter greeting/farewell cases return templates directly without Prolog engine — they always succeed
  - Language code uses 2-char prefix (e.g., 'fr' from 'fr-FR') for template lookup
---

## 2026-04-06 - US-005 (chat-pipeline-fixes)
- Added French fallback templates to NPC-NPC conversation engine (4 topics: greeting, work, gossip, weather)
- Changed FALLBACK_TEMPLATES from array to Record<string, FallbackTemplate[]> keyed by language code
- Added getFallbackTemplates() helper with English fallback for unsupported languages
- Updated generateFallbackConversation() to accept targetLanguage parameter
- Passed languageUsed from initiateConversation() to all generateFallbackConversation() calls
- Added 3 tests: French templates produce French text, English default, unsupported language fallback
- Files changed: server/services/conversation/npc-conversation-engine.ts, server/__tests__/npc-npc-conversation-endpoint.test.ts
---

## 2026-04-06 - US-006 (chat-pipeline-fixes)
- Created classifyMessageComplexity() function: simple if <15 words AND >2 history AND no question/quest keywords
- Added modelOverride to StreamCompletionOptions interface
- Updated GeminiStreamingProvider to use modelOverride when provided
- Wired tiered routing into http-bridge.ts and ws-bridge.ts: simple → FLASH, complex → PRO
- Added logging for routing decisions
- Added 9 classification tests covering various edge cases
- Files changed: server/services/conversation/{http-bridge.ts,ws-bridge.ts,providers/llm-provider.ts,providers/gemini-provider.ts}, server/__tests__/message-complexity.test.ts
- **Learnings for future iterations:**
  - classifyMessageComplexity is exported from http-bridge.ts and imported by ws-bridge.ts
  - GEMINI_MODELS.FLASH = 'gemini-3.1-flash-lite-preview', GEMINI_MODELS.PRO = 'gemini-3.1-pro-preview'
---

## 2026-04-06 - US-007 (chat-pipeline-fixes)
- Wired compressConversationHistory() into http-bridge.ts and ws-bridge.ts
- After storing response in history, check if session.history.length > 20; if so, convert to GeminiMessage format, compress, and convert back
- Fixed pre-existing import path bug in conversation-compression.ts: `../config/gemini.js` → `../../config/gemini.js`
- Compression is non-fatal — failure only logs a warning
- Files changed: server/services/conversation/{http-bridge.ts,ws-bridge.ts,conversation-compression.ts}
- **Learnings for future iterations:**
  - conversation-compression.ts uses GeminiMessage format (role: 'user'|'model', parts) while bridges use {role: 'user'|'assistant', content} — conversion needed
  - The compression function returns messages unchanged if below threshold or if Gemini is unavailable (no API key in test env)
---

## 2026-04-06 - US-008 (chat-pipeline-fixes)
- Added fire-and-forget relationship update to http-bridge.ts and ws-bridge.ts after each response
- Formula: interactionQuality = 0.02 + (agreeableness * 0.03) * (exchangeCount / 5)
- Uses dynamic import of social-dynamics-system.js to avoid loading MongoDB at module level
- Update is fire-and-forget (Promise chain with .catch) — failure only logs a warning
- Files changed: server/services/conversation/{http-bridge.ts,ws-bridge.ts}
- **Learnings for future iterations:**
  - Dynamic import of social-dynamics-system.js triggers MongoDB connection — must be fire-and-forget to avoid blocking in tests
  - Never await the relationship update import in the response path — it will timeout in test env
---

## 2026-04-06 - US-009 (chat-pipeline-fixes) - Empty LLM Response Detection
- Added empty response detection to http-bridge.ts and ws-bridge.ts
- After LLM streaming completes, check if fullResponse is empty/whitespace
- If empty: log warning, send SSE/WS error event, send fallback text ("lost my train of thought"), set fullResponse to fallback
- If not empty: proceed normally with TTS and final text marker
- Added EmptyLLMProvider mock and test verifying error event, fallback text, and done completion
- Files changed: server/services/conversation/{http-bridge.ts,ws-bridge.ts}, server/__tests__/ws-bridge.test.ts
---
