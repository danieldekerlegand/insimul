## Agent 26: Add animal NPCs (cats, dogs, birds) for world ambiance and vocabulary

### Completed
- Created AnimalNPCSystem.ts: procedural spawning of cats, dogs, and birds with wandering/flying behaviors
  - Procedural mesh builders for each species (body, head, limbs, wings, etc.)
  - Multiple color variants per species for visual variety
  - Behavior system: idle, wander, sit (ground animals), fly (birds)
  - Vocabulary integration: each animal tagged with word and category for language learning
  - Helper functions: getAnimalsNear(), getAnimalVocabulary() for game system integration
- Added AnimalIR type to shared/game-engine/ir-types.ts for engine-agnostic export
- Expanded animals vocabulary corpus (shared/language/vocabulary-corpus.ts):
  - Added animal body parts: tail, wing, feather, paw, beak, nest
  - Added behavior verbs: to fly, to run, to sit, to bark, to sing, to purr, to wander
  - Added descriptors: small, fast, wild, tame
  - Added young animals: puppy, kitten
- Integrated AnimalNPCSystem into BabylonGame.ts (init, spawn, per-frame update, dispose)
- Wrote 45 tests covering pure functions, species configs, vocabulary integration
- All tests pass, no new TypeScript or lint errors

## Agent 12: Implement server-side persistence for vocabulary and grammar progress

### Completed
- Added vocabulary/grammar/progress storage methods to IStorage interface (server/db/storage.ts)
- Added individual REST endpoints for vocabulary and grammar CRUD (server/routes/telemetry-routes.ts):
  - GET /api/vocabulary/:playerId/:worldId
  - PUT /api/vocabulary/:playerId/:worldId/:word
  - GET /api/grammar-patterns/:playerId/:worldId
  - PUT /api/grammar-patterns/:playerId/:worldId/:pattern
- Added loadFromServer() method to LanguageProgressTracker (client) to restore persisted progress on initialization
- Wired up startServerSync (60s periodic sync) and loadFromServer in BabylonChatPanel initialization
- Added cleanup: final sync + stopServerSync on BabylonChatPanel dispose
- Wrote 17 tests covering all endpoints including error handling (server/__tests__/language-progress-persistence.test.ts)
- All tests pass, TypeScript compiles cleanly
