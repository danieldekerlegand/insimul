## Codebase Patterns
- MongoDB is the primary DB (not PostgreSQL) — Mongoose in server/db/mongo-storage.ts, Drizzle types in shared/schema.ts
- Storage is imported as singleton: `import { storage } from '../../db/storage'` — mock with `vi.mock('../../db/storage')`
- vitest is configured at project root (vitest.config.ts), run with `npx vitest run <path>`
- Pre-existing TypeScript errors (~459 after US-003) — don't try to fix unrelated ones, only verify you don't add new ones
- Map iteration in TypeScript: use `Array.from(map)` instead of `for...of` on Maps when target < es2015
- IStorage interface is in server/db/storage.ts; MongoStorage must implement all methods declared there
- Occupation CRUD uses OccupationModel (Mongoose) — schema added in US-002
- TotT modules are in server/extensions/tott/ — each exports functions, not classes
- Server starts with: NODE_OPTIONS='--max-old-space-size=4096' NODE_ENV=development tsx -r dotenv/config server/index.ts
- 60 unit tests already pass in server/__tests__/remaining-gaps.test.ts
- tau-prolog is used for Prolog engine (must be in package.json)
- Building lifecycle has TWO implementations that need merging: building-commission-system.ts and building-lifecycle.ts
- For deterministic tests, mock Math.random with Mulberry32 seeded PRNG: `Math.random = mulberry32(seed)`
- Use `vi.hoisted()` to create mock objects referenced inside `vi.mock()` factories — plain `const` declarations are not available at hoist time
- Telemetry routes use dependency injection (createTelemetryRoutes(storage)) — test by passing mock storage directly, no vi.mock needed
- building-commission-system.ts imports from hiring-system, business-system, and prolog-queries for lifecycle functions
- Hi-fi simulation imports 13 TotT modules — all must be mocked: routine, autonomous-behavior, knowledge, social-dynamics, lifecycle, grieving, economics, building-commission, town-events, drama-recognition, appearance, artifact, education
- InsimulRuleCompiler mock must also use `function` keyword (same constructor pattern as PrologManager)
- The merged lifecycle functions have different signatures than the old simplified ones (e.g., constructBuilding takes worldId/lotId/businessId instead of commissionerId/settlementId)
- For simulation integration tests: mock storage singleton via `vi.mock('../db/storage')`, mock Prolog modules, and mock TotT extension modules (lazy-imported via dynamic import)
- PrologManager mock must use `function` keyword (not arrow fn) for `new` to work in vitest
- Changing ir-types.ts or types.ts triggers pre-commit drift check — update all 4 export templates + `npm run drift:update` before commit
- Unity template file paths use lowercase: scripts/data/ and scripts/core/ (not Scripts/Data/)
---

## 2026-03-11 - US-001
- Merged building-lifecycle.ts into building-commission-system.ts
- Kept superior implementations from building-lifecycle.ts: Prolog fact assertions, truth creation for succession events, seniority-based employee succession, family successor search via relationships
- Deleted server/extensions/tott/building-lifecycle.ts
- Updated server/extensions/tott/index.ts comment
- Added imports: getBusinessEmployees, closeBusiness, transferOwnership, prologAssertFact
- Added types: ConstructionResult, DemolitionResult, RenovationResult, SuccessionResult
- Files changed: building-commission-system.ts, index.ts, building-lifecycle.ts (deleted)
- **Learnings for future iterations:**
  - No code anywhere imported from building-lifecycle.ts — it was only referenced by comments in index.ts
  - The lifecycle functions have different API signatures in the merged version vs the old simplified ones
  - TypeScript error count remains at 480 pre-existing (no new errors added)
---

## 2026-03-11 - US-002
- Added OccupationSchema + OccupationModel to mongo-storage.ts
- Implemented 8 missing IStorage methods: getBusinessesByWorld, getOccupation, getOccupationsByCharacter, getOccupationsByBusiness, getCurrentOccupation, createOccupation, updateOccupation, deleteOccupation
- Removed stale TODO comment (3 of 8 listed methods already existed: getBaseRulesByCategory, getActionsByType, getBaseActions)
- Files changed: server/db/mongo-storage.ts
- **Learnings for future iterations:**
  - The TODO in MongoStorage was partially stale — always verify which methods are actually missing before implementing
  - IStorage errors cascade: 11 direct errors → 16 total error reduction (480→464)
  - PRD estimated "at least 40" IStorage errors but actual count was 11 direct + 5 cascade = 16 total
  - Occupation model uses simple `any` return types matching the Lot/Residence/Business pattern (no typed Doc interface)
---

## 2026-03-11 - US-003
- Fixed 5 TypeScript errors in server/engines/unified-engine.ts
- Lines 306, 325: Converted `for...of` Map iteration to `Array.from(this.rules)` pattern
- Line 662: Added `?? 0` default for nullable `birthYear` in CharacterSnapshot
- Line 664: Added `?? true` default for nullable `isAlive` in CharacterSnapshot
- Line 694: Changed `rule.parsedContent` to `(rule as any).parsedContent` to fix property access error
- Files changed: server/engines/unified-engine.ts
- **Learnings for future iterations:**
  - TypeScript error count: 464 → 459 (5 errors fixed)
  - Map iteration requires `Array.from()` wrapper when tsconfig target < es2015
  - Schema types from shared/schema.ts have many nullable fields — use `?? defaultValue` when assigning to non-nullable targets
  - `parsedContent` is not in the Rule type definition — access via `(rule as any).parsedContent`
---

## 2026-03-11 - US-004
- Created server/__tests__/simulation-integration.test.ts with 4 lo-fi simulation tests
- Mocked: storage singleton (vi.mock('../db/storage')), Prolog modules (prolog-sync.js, prolog-manager.js), all 6 TotT extension modules (autonomous-behavior, lifecycle, grieving, building-commission, town-events, appearance)
- Tests verify: success=true, deaths > 0, marriages > 0, valid rate table returned
- 20 seeded characters with mixed ages (19-89), genders, and 3 married couples
- Files changed: server/__tests__/simulation-integration.test.ts (new)
- **Learnings for future iterations:**
  - PrologManager mock must use `function` keyword (not arrow fn) since it's instantiated with `new` — vitest warns about this
  - syncWorldToProlog fails gracefully in simulateLoFi (caught + warned, simulation continues) — no need to make Prolog mock perfect
  - executeSimulationTimestep's TypeScript return type omits `lifeEvents` but the actual runtime return includes it — simulateLoFi accesses `simResult.lifeEvents` via `any` cast
  - TotT modules import storage as singleton from `../../db/storage` — must mock that module globally for integration tests
  - simulateLoFi uses dynamic `import()` for TotT modules — vi.mock works on these paths
  - 140-year simulation with 3.6% sampling = ~1,840 timesteps — tests complete in <200ms with mocks
---

## 2026-03-11 - US-005
- Added 3 hi-fi simulation integration tests to server/__tests__/simulation-integration.test.ts
- Added mocks for 7 additional TotT modules: routine-system, knowledge-system, social-dynamics-system, economics-system, drama-recognition-system, artifact-system, education-system
- Added decayMorale to town-events-system mock, extra exports to autonomous-behavior-system mock
- Added InsimulRuleCompiler mock (unified-syntax.js)
- Fixed PrologManager mock to use `function` keyword and added `addRule` method
- Tests verify: success=true, 10 stepResults, at least one aggregate counter > 0
- Files changed: server/__tests__/simulation-integration.test.ts
- **Learnings for future iterations:**
  - Hi-fi simulation imports 13 TotT modules via dynamic import — all need mocking
  - executePrologStep also imports InsimulRuleCompiler from client/src/lib/unified-syntax.js — needs mock with `function` keyword constructor
  - The mock for autonomous-behavior-system's executeSimulationTimestep returns observations/socializations arrays — hi-fi counts their lengths for totalObservations/totalSocializations
  - All errors in hi-fi subsystems are caught and warned — simulation continues even if individual systems fail
  - 10-step hi-fi simulation completes in ~190ms with all mocks
---

## 2026-03-11 - US-006
- Added determinism test to server/__tests__/simulation-integration.test.ts
- Implemented Mulberry32 seeded PRNG to replace Math.random for deterministic runs
- Test runs simulateLoFi() twice with identical seed (42), verifies all totalLifeEvents fields match exactly
- Also verifies at least some deaths occurred (sanity check)
- Files changed: server/__tests__/simulation-integration.test.ts
- **Learnings for future iterations:**
  - Math.random is the only source of non-determinism in lo-fi simulation — the mocked TotT modules are already deterministic (timestepCallCount-based)
  - Mulberry32 is a simple, fast seeded PRNG suitable for test determinism — `function mulberry32(seed: number): () => number`
  - Must reset `timestepCallCount`, `deathCount`, `marriageCount` between runs since those are module-level counters shared across tests
  - Each run needs a fresh InsimulSimulationEngine instance and fresh mockStorage to avoid shared state
---

## 2026-03-11 - US-007
- Created server/__tests__/rate-distribution.test.ts with 18 unit tests
- Tests cover: Bernoulli trial distribution (birthRate=0.18 over 10k trials within 2σ), deathRateMultiplier comparison (medieval 1.4 vs default 1.0), getWorldTypeDefaults for all 4 world types + null/undefined/unknown, fuzzy matching (cyberpunk→sci-fi, fantasy→medieval, contemporary→modern, ancient/colonial→historical), case-insensitive and whitespace-padded input, getRegisteredWorldTypes
- Files changed: server/__tests__/rate-distribution.test.ts (new)
- **Learnings for future iterations:**
  - world-type-defaults.ts is a pure module with no dependencies — ideal for unit testing with no mocks needed
  - Seeded PRNG (LCG: `seed * 1664525 + 1013904223`) is sufficient for reproducible statistical tests
  - getWorldTypeDefaults returns copies via spread operator — tests can verify no reference sharing
  - Fuzzy matching uses both `includes()` checks: `normalized.includes(key) || key.includes(normalized)` plus explicit alias lists
---

## 2026-03-11 - US-008
- Created server/__tests__/business-lifecycle.test.ts with 11 integration tests
- Tests cover: constructBuilding (vacant lot update, business linking, occupied lot error, missing lot error), demolishBuilding (vacant return + formerBuildingIds, active business rejection, closed business lotId clearing), handleBusinessSuccession (spouse succession, child succession, employee succession, business closure when no candidate)
- Mocked: storage singleton via vi.hoisted + vi.mock, personality-behavior-system, hiring-system, business-system, prolog-queries
- Files changed: server/__tests__/business-lifecycle.test.ts (new)
- **Learnings for future iterations:**
  - Must use `vi.hoisted()` to create mock objects referenced in `vi.mock()` factories — vitest hoists mock calls above variable declarations, so `const mockX = { ... }; vi.mock('...', () => ({ x: mockX }))` fails with "Cannot access before initialization"
  - building-commission-system.ts's `constructBuilding` uses lotId as buildingId when no businessId is provided (residential construction)
  - `findLotByBuildingId` iterates all settlements → lots to find a lot by buildingId — tests must mock getSettlementsByWorld and getLotsBySettlement
  - `handleBusinessSuccession` creates truth entries via storage.createTruth — must mock that too
  - TypeScript error count unchanged at 459
---

## 2026-03-11 - US-009
- Created server/__tests__/telemetry-batch.test.ts with 5 tests for POST /external/telemetry/batch
- Tests cover: missing API key (401), invalid API key (403), valid key with well-formed events (200), mixed valid/malformed events with rejection details, empty body with valid key
- Used lightweight HTTP request helper (no supertest) — creates Express app with createTelemetryRoutes(mockStorage) and makes real HTTP requests
- Mocked storage methods: validateApiKey, createTelemetryBatch, createEngagementBatch
- Verified events are enriched with worldId and source='external' from key record
- Files changed: server/__tests__/telemetry-batch.test.ts (new)
- **Learnings for future iterations:**
  - createTelemetryRoutes() accepts storage as parameter (not singleton import) — much easier to test than modules using singleton storage
  - External endpoint returns 401 for missing key, 403 for invalid key (not both 401)
  - Malformed events are individually rejected with reasons array, valid ones still processed — partial success pattern
  - No supertest in project; lightweight http.createServer + http.request works fine for route testing
  - TypeScript error count unchanged at 459
---

## 2026-03-11 - US-010
- Added ContentValidationIndicator to ActionsHub and QuestsHub editors (were missing, RulesHub already had it)
- Verified all other acceptance criteria via code inspection: WorldCreateDialog timestep config, ItemsHub lore truths/quest relevance/language learning sections, GrammarsHub truth bindings/context type selector, LanguagesHub cultural context linked truths
- No browser testing tools available — manual browser verification needed
- Files changed: client/src/components/actions/ActionsHub.tsx, client/src/components/quests/QuestsHub.tsx
- **Learnings for future iterations:**
  - ContentValidationIndicator pattern: import component + validator, add useMemo for validation result, render `<ContentValidationIndicator>` in a `px-4 pb-2 shrink-0` div
  - validateActionContent and validateQuestContent are exported from `@shared/prolog/content-validators`
  - ActionsHub and QuestsHub follow same 3-panel layout as RulesHub but were missing the validation badge
  - TypeScript error count unchanged at 459
---

## 2026-03-11 - US-011
- Wired ManualHistoryEditor and FamilyTreeView into WorldIntelligenceTab via HistorySubTabs component
- History tab now has 3 sub-tabs: Timeline (HistoryTimelineView), Editor (ManualHistoryEditor), Family Tree (FamilyTreeView)
- Verified all TruthTab features via code inspection: list/timeline/graph toggles, HorizontalTimeline, TruthGraph, CausalChainOverlay
- No browser testing tools available — manual browser verification needed
- Files changed: client/src/components/WorldIntelligenceTab.tsx
- **Learnings for future iterations:**
  - ManualHistoryEditor and FamilyTreeView existed as standalone components but were not imported anywhere — always check if new components are wired into parent views
  - WorldIntelligenceTab uses nested Tabs components — inner tabs work fine with Radix UI tabs
  - FamilyTreeView uses SVG with transform-based zoom/pan; GenealogyView is DOM-based — two different tree implementations exist
  - TruthTab already had all 3 view modes (list/timeline/graph) fully wired with HorizontalTimeline, TruthGraph, and CausalChainOverlay
  - TypeScript error count unchanged at 459
---

## 2026-03-11 - US-012
- Verified all 6 acceptance criteria via code inspection (no browser testing tools available)
- KB Query (EnhancedKBQuery.tsx): category grouping (8 categories), query builder with predicate selector, history panel with localStorage persistence, suggestions panel with 18 pre-defined queries
- SimulationsView: collapsible rate tables editor with 10 editable number fields, reset button, modification badges
- ResearcherDashboard: all 8 tabs present (overview, instruments, technical, engagement, sessions, language, participants, export)
- TelemetryMonitorDashboard: activity tab (hourly bar chart), errors tab (error count/rate), players tab (per-player sessions)
- EngineExportDialog: telemetry toggle switch, server URL input, API key dropdown selector
- BabylonChatPanel: 5-star rating row (createRatingRow method) displayed after NPC messages with gold hover/selection styling
- No code changes needed — all components already fully implemented
- Files changed: ralph/prd.json (marked passes: true)
- **Learnings for future iterations:**
  - No browser testing tools configured — all browser QA stories verified via code inspection only
  - EnhancedKBQuery uses localStorage for query history persistence
  - TelemetryMonitorDashboard has 6 tabs total (overview, API keys, live sessions, activity, errors, players) — more than the 3 checked
  - BabylonChatPanel star ratings use callback pattern: setOnDialogueRating(callback) to register handler
---

## 2026-03-12 - US-001 (naturalized-procgen)
- Created shared/procedural/noise.ts with seeded 2D Simplex noise implementation
- Created shared/procedural/noise.test.ts with 18 unit tests
- Implementation: createNoise2D(seed: string) => (x, y) => number in [-1, 1]
- fractalNoise(noise, x, y, octaves, lacunarity, persistence) => normalized fBm value
- Seeding uses string hash → xorshift32 PRNG → Fisher-Yates shuffle permutation table
- All tests pass: determinism (same seed = same values), different seeds diverge, output range [-1,1], fractal noise
- Files changed: shared/procedural/noise.ts (new), shared/procedural/noise.test.ts (new)
- **Learnings for future iterations:**
  - Project uses simple assert-based test pattern (not vitest) for shared/ modules — see tau-engine.test.ts pattern
  - `npx tsx` runs TypeScript directly; `npx tsc --noEmit` for typecheck
  - Simplex noise scaling factor of 70 keeps output within [-1, 1]
  - fractalNoise normalizes by dividing by sum of amplitudes to keep output near [-1, 1]
---

## 2026-03-12 - US-002 (naturalized-procgen)
- Created server/generators/terrain-generator.ts with TerrainGenerator class
- Created server/generators/terrain-generator.test.ts with 31 unit tests
- TerrainConfig: seed, width, height, terrainType (7 types), resolution (default 128)
- Terrain noise params: plains (0.05/3oct), hills (0.15/4oct), mountains (0.40/6oct), coast (0.08/3oct), river (0.10/4oct), forest (0.08/3oct), desert (0.12/2oct)
- Resolution by settlement size: village=128, town=256, city=512
- Post-processing: coast gets land-to-sea gradient, river gets meandering center channel
- All tests pass, typecheck unchanged at 459 pre-existing errors
- Files changed: server/generators/terrain-generator.ts (new), server/generators/terrain-generator.test.ts (new)
- **Learnings for future iterations:**
  - Heightmap normalization: base elevation 0.5 ± amplitude keeps values in [0,1] after clamping
  - Coast/river terrain types need post-processing modifiers beyond base noise
  - Test pattern for generators: use stats helper (min/max/mean/stddev) to verify distribution properties
  - TerrainGenerator is a pure class with no storage dependencies — easy to test without mocks
---

## 2026-03-12 - US-003 (naturalized-procgen)
- Added 6 stamp methods to TerrainGenerator: stampPeak (Gaussian bump), stampValley (inverse Gaussian), stampCanyon (narrow deep cut), stampCliff (sigmoid step), stampMesa (flat-topped plateau), stampCrater (bowl + rim)
- Added stampFeatures(heightmap, terrainType, seed) auto-placement: places 2-5 terrain-appropriate features with generated names
- Added TerrainFeature interface and FeatureType type, FEATURE_NAMES and FEATURES_BY_TERRAIN lookup tables
- 72 new test assertions (103 total) covering all 6 stamp methods, feature count/properties/terrain-appropriateness/determinism/range
- Files changed: server/generators/terrain-generator.ts, server/generators/terrain-generator.test.ts
- **Learnings for future iterations:**
  - Cliff sigmoid needs `exp(dz * sharpness)` (positive sign) for "high above, low below" — negative sign makes both sides equal at test points
  - stampFeatures uses noise-based deterministic random for count/type/position/name selection — no Math.random needed
  - Each stamp method clamps output to [0,1] via Math.max(0, Math.min(1, ...))
  - Feature types per terrain: plains=[valley,mesa], hills=[peak,valley,cliff], mountains=[peak,canyon,cliff,crater], coast=[cliff,valley], river=[canyon,valley], forest=[valley,peak], desert=[mesa,canyon,crater]
  - TypeScript error count unchanged at 459
---

## 2026-03-12 - US-004 (naturalized-procgen)
- Added deriveSlopeMap(heightmap): gradient magnitude at each cell using central differences (forward/backward at edges)
- Added deriveAspectMap(heightmap): angle of steepest descent in radians [0, 2π)
- Added static isBuildable(slopeMap, x, z, maxSlope=0.52): checks slope angle < 30°
- Added static getRoadCost(slopeMap, x, z): 1.0 flat, exponential increase, Infinity above ~15°
- 7 new test functions covering flat/ramp slopes, aspect directions, buildability, road cost, generated heightmap comparison
- All 669 tests pass, typecheck unchanged at 459
- Files changed: server/generators/terrain-generator.ts, server/generators/terrain-generator.test.ts
- **Learnings for future iterations:**
  - Slope map stores gradient magnitude (not angle) — convert via Math.atan() when comparing to angle thresholds
  - Central differences: (f[i+1] - f[i-1]) / 2 for interior, forward/backward difference at edges
  - isBuildable and getRoadCost are static methods since they don't use generator state
  - Plains mean slope (~0.015) vs mountains (~0.103) — useful for validating terrain type differentiation
---

## 2026-03-12 - US-005 (naturalized-procgen)
- Added TerrainFeatureIR interface to ir-types.ts with 10 feature types, position Vec3, radius, elevation, description
- Added terrainFeatures[] and slopeMap? fields to GeographyIR
- Added elevation (integer) and slopeProfile (text) to settlements table in shared/schema.ts
- Created terrain_features table in shared/schema.ts with full Drizzle definition + insert schema + types
- Added TerrainFeatureSchema/Model to mongo-storage.ts with worldId index
- Added elevation/slopeProfile to SettlementSchema in mongo-storage.ts
- Added 5 CRUD methods to IStorage and MongoStorage: get, getByWorld, create, update, delete
- Fixed ir-generator.ts to include terrainFeatures: [] in geography output
- Files changed: shared/game-engine/ir-types.ts, shared/schema.ts, server/db/storage.ts, server/db/mongo-storage.ts, server/services/game-export/ir-generator.ts
- **Learnings for future iterations:**
  - Adding required fields to IR interfaces breaks ir-generator.ts — always provide defaults there
  - Mongoose schemas use Schema.Types.Mixed for JSONB-like position objects
  - TerrainFeature CRUD follows same pattern as Item CRUD (simple findById, find by worldId)
  - TypeScript error count unchanged at 459
---

## 2026-03-12 - US-006 (naturalized-procgen)
- Created client/src/components/3DGame/TerrainRenderer.ts with class TerrainRenderer
- createTerrainMesh(heightmap, terrainSize, scene, maxHeight) generates subdivided ground mesh with vertex Y from heightmap
- Biome-specific vertex coloring: green (low) → brown (mid) → gray (high) → white (peaks)
- Terrain mesh has receiveShadows=true, checkCollisions=true, isPickable=true
- Added loadGeography() to DataSource interface, ApiDataSource, and FileDataSource
- Integrated into BabylonGame.ts: tryApplyTerrainHeightmap() called after loadWorldData, replaces flat ground when heightmap available
- Added 'terrain' to SettlementSceneManager globalNames set for visibility
- Flat ground fallback preserved — terrain only applied when heightmap data exists
- Files changed: client/src/components/3DGame/TerrainRenderer.ts (new), client/src/components/3DGame/BabylonGame.ts, client/src/components/3DGame/DataSource.ts, ralph/prd.json
- **Learnings for future iterations:**
  - Babylon.js CreateGround produces (subdivisions+1)^2 vertices in a grid — map world coords to heightmap via (pos/size + 0.5)
  - VertexData.ComputeNormals must be called after modifying vertex positions for correct lighting
  - StandardMaterial automatically uses vertex colors when vertex color buffer is present
  - Ground mesh is registered as global in SettlementSceneManager via name matching ('ground' in globalNames set)
  - DataSource interface must be updated in all 3 places: interface, ApiDataSource, FileDataSource
  - BabylonGame flow: initializeScene → createGround (flat) → loadWorldData → tryApplyTerrainHeightmap (replaces if heightmap available)
  - TypeScript error count unchanged at 459
---

## 2026-03-12 - US-007 (naturalized-procgen)
- Added StreetNode, StreetEdge, StreetNetwork interfaces to shared/game-engine/types.ts
- Added StreetNodeIR, StreetEdgeIR, StreetNetworkIR interfaces to shared/game-engine/ir-types.ts
- Added optional streetNetwork field to SettlementIR
- Updated all 4 export data generators (Babylon, Godot, Unity, Unreal) to pass streetNetwork data
- Added InsimulStreetNetworkData/NodeData/EdgeData C# classes to Unity InsimulSettlementData.cs
- Added FInsimulStreetNode/Edge/Network USTRUCTs + EInsimulStreetNodeType/StreetType enums to Unreal GameTypes.h
- Added FInsimulStreetNetwork field to Unreal SettlementData.h
- Updated drift manifest to match new hashes
- Files changed: shared/game-engine/types.ts, shared/game-engine/ir-types.ts, godot-data-generator.ts, unity-data-generator.ts, unreal-datatable-generator.ts, InsimulSettlementData.cs, GameTypes.h, SettlementData.h, drift-manifest.json
- **Learnings for future iterations:**
  - Changing ir-types.ts or types.ts triggers pre-commit drift check — must update all 4 export templates + run `npm run drift:update`
  - Unity template files are at lowercase paths: scripts/data/ and scripts/core/ (not Scripts/Data/) — macOS case-insensitive FS masks this
  - Godot/Babylon generators pass raw dictionaries so new optional fields flow through automatically
  - Unity needs explicit C# [Serializable] classes; Unreal needs USTRUCT(BlueprintType) with GENERATED_BODY()
  - SettlementData.h needs #include "GameTypes.h" to use FInsimulStreetNetwork
  - TypeScript error count unchanged at 459
---

## 2026-03-12 - US-008 (naturalized-procgen)
- Created server/generators/street-generator.ts with class StreetGenerator
- Method generateOrganic(config: StreetGenConfig): StreetNetwork generates connected organic/medieval street graphs
- Algorithm: center node → 3-5 main roads at irregular angles → secondary branches → tertiary dead-end connections → connectivity enforcement
- Perlin noise displacement applied to waypoints for organic curves
- Slope map avoidance: nudges road nodes away from steep terrain (>15°) via random sampling
- Connectivity guaranteed via BFS + MST-style bridge edges for disconnected components
- Street types: main_road (width 8), residential (width 6), lane (width 4)
- Created server/generators/street-generator.test.ts with 320 assertions
- Tests cover: basic structure, node/edge validity, full connectivity (BFS), determinism, different seeds, street types, slope map avoidance, larger settlements
- Files changed: server/generators/street-generator.ts (new), server/generators/street-generator.test.ts (new)
- **Learnings for future iterations:**
  - Set iteration requires `Array.from()` wrapper (same as Map iteration) when target < es2015
  - StreetGenerator is a pure class with no storage dependencies — easy to test without mocks
  - Organic pattern: main roads radiate from center, secondary branch perpendicular, tertiary connect nearby dead ends
  - findNearbyNode snapping (radius * 0.08 threshold) prevents duplicate nodes at intersections
  - ensureConnectivity must also update adjacency list when adding bridge edges for multi-component graphs
  - TypeScript error count unchanged at 459
---

## 2026-03-12 - US-009 (naturalized-procgen)
- Added generateGrid() method to StreetGenerator for planned/grid-based street layouts
- Grid dimensions computed from settlement radius (blockSize=40 units)
- Noise perturbation (+/-3.5% of block size) applied to each intersection for imperfection
- Every Nth row/column (default 4th) marked as main boulevard (wider streets)
- Diagonal avenues: 1 diagonal for pop > 3000, 2 diagonals for pop > 8000
- Grid axis rotation via optional gridRotation direction vector
- Slope map avoidance reused from organic generator
- 1581 total test assertions pass (organic + grid), typecheck unchanged at 459
- Files changed: server/generators/street-generator.ts, server/generators/street-generator.test.ts
- **Learnings for future iterations:**
  - generateGrid accepts extended config: population, gridRotation, mainInterval — uses intersection type for config extension
  - Grid (N+1)*(M+1) nodes with N*M blocks; edges = horizontal rows + vertical columns + optional diagonals
  - Perturbation uses noise() not rng() — ensures deterministic perturbation independent of node processing order
  - Diagonal avenues reuse existing grid nodes (no new nodes), just add connecting edges between them
  - TypeScript error count unchanged at 459
---

## 2026-03-12 - US-010 (naturalized-procgen)
- Added generateRadial() method to StreetGenerator for capital cities and fortress towns
- Central plaza node with 6-8 radial boulevards (seeded random count) emanating outward at evenly-spaced angles
- 2-4 concentric ring roads at increasing radii (15%-90% of settlement radius) connecting radials
- Noise-based lateral displacement on radial road waypoints for slight curves
- Inner ring edges are boulevard (main_road); outer ring edges are residential
- Radial endpoints beyond outermost ring for edge-of-settlement roads
- Slope map avoidance reused from existing infrastructure
- 1997 total test assertions pass (organic + grid + radial), typecheck unchanged at 459
- Files changed: server/generators/street-generator.ts, server/generators/street-generator.test.ts
- **Learnings for future iterations:**
  - Radial pattern creates nodes in a 2D grid: radialNodes[ringIndex][radialIndex] for easy ring edge creation
  - Ring road tier assignment: `ri < ringCount / 2 ? 'main' : 'secondary'` splits inner/outer rings
  - Ring radii formula: `radius * (0.15 + ((r+1)/(ringCount+1)) * 0.75)` gives even spacing from 15% to 90% of radius
  - radialCount (6-8) and ringCount (2-4) produce 19-41 nodes total including endpoints
  - TypeScript error count unchanged at 459
---
