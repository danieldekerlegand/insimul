# Ralph Progress Log
Started: Sun Mar 29 13:53:35 CDT 2026
---

## 2026-04-03 - Audit: Vocabulary System — Spaced Repetition, Word Tracking, and Reward Integration
- **Audit findings**: 3 issues found in vocabulary system:
  1. **Mastery calculation mismatch**: `calculateMasteryLevel()` in `progress.ts` used different thresholds (10 correct + 15 encounters for mastered) than `getMasteryForCorrectCount()` in `vocabulary-review.ts` (8 correct). A word could be "mastered" in SRS review but only "familiar" in LanguageProgressTracker.
  2. **Hardcoded vocabulary category**: `GameTruthSync.handleVocabularyUsed()` always used `category = 'general'` instead of deriving from the event, so Prolog `knows_vocabulary` assertions always got wrong category.
  3. **vocabulary_used event missing category**: The `GameEvent` type for `vocabulary_used` didn't include a `category` field, preventing downstream handlers from getting the word's category.
- **Fixes applied**:
  - Updated `calculateMasteryLevel()` to use same thresholds as SRS (3/5/8 correct uses for learning/familiar/mastered), with encounter count gating only new→learning transition
  - Added optional `category` field to `vocabulary_used` event type in `GameEventBus.ts`
  - Updated `GameTruthSync.handleVocabularyUsed()` to use `event.category` when available, falling back to 'general'
  - Updated existing tests in `e2e-main-quest-systems.test.ts` and `e2e-world-validation.test.ts` to match aligned thresholds
- **Tests added**: 36 new audit tests in `shared/__tests__/vocabulary-system-audit.test.ts` covering:
  - Mastery calculation alignment between both code paths
  - SRS scheduling intervals and review selection
  - SRS quest objective generation
  - XP reward integration
  - Vocabulary category unlock flow
  - Event pipeline category propagation
  - End-to-end word tracking → SRS → quest → reward flow
- **Noted gap**: `calculateReviewXPBonus()` exists in `srs-quest-objectives.ts` but is never called during quest completion — vocabulary review quests don't get per-word mastery XP bonus
- Files changed:
  - `shared/language/progress.ts` — aligned `calculateMasteryLevel()` thresholds with SRS system
  - `shared/game-engine/logic/GameEventBus.ts` — added `category?` to `vocabulary_used` event type
  - `shared/game-engine/logic/GameTruthSync.ts` — use event category in `handleVocabularyUsed()`
  - `shared/__tests__/vocabulary-system-audit.test.ts` — new: 36 audit tests
  - `shared/__tests__/e2e-main-quest-systems.test.ts` — updated mastery threshold expectations
  - `shared/__tests__/e2e-world-validation.test.ts` — updated mastery threshold expectations
- **Learnings for future iterations:**
  - Two parallel mastery calculation paths exist: `getMasteryForCorrectCount` (SRS) and `calculateMasteryLevel` (LanguageProgressTracker) — keep them aligned
  - `vocabulary_used` events flow through GameEventBus to GameTruthSync, GamePrologEngine, QuestObjectManager, and PlayerActionTracker
  - Category unlock persists in `playerProgress.saveData.unlockedVocabularyCategories`
  - 9 pre-existing test failures in e2e-main-quest-systems and e2e-world-validation (main quest chapter/book seed issues, not vocabulary-related)
---

## 2026-04-03 - Audit: Radiant Quest Generation and Assignment
- **Audit findings**: Radiant (procedural) quest distribution had 3 bugs preventing side quests from reaching NPCs:
  1. `distributeRadiantQuests()` filtered for `status === 'active'` but generators (side-quest, fetch, reading) create quests with `status: 'available'` — quests were never found for distribution
  2. Distribution redundantly set `status: 'available'` which would downgrade any 'active' quest it accidentally matched, removing it from the player's quest log
  3. `_generateContextualQuests` threshold only counted 'active' quests, so 'available' unassigned quests accumulated without limit
- **Fixes applied**:
  - Changed `distributeRadiantQuests()` filter from `status === 'active'` to `status === 'available'` (line 541)
  - Removed redundant `status: 'available'` from the update call — distribution now only sets `assignedByCharacterId` and `assignedBy`
  - Added pending-distribution count to `_generateContextualQuests` threshold: `activeCount + pendingCount >= 8`
- **Tests added**: 15 tests covering distribution, lifecycle, generator output shape, and accumulation guard
- Files changed:
  - `shared/game-engine/logic/GameQuestManager.ts` — fixed distributeRadiantQuests status filter, removed status downgrade, added pending count to threshold
  - `shared/__tests__/radiant-quest-audit.test.ts` — 15 new audit tests
- **Learnings for future iterations:**
  - Side-quest-generator creates quests with `status: 'available'` and `assignedByCharacterId` already set (NPC picked by occupation)
  - Other generators (shopping, crafting, number-practice) create quests with `status: 'active'` — these skip NPC distribution and go straight to player's quest log
  - `distributeRadiantQuests` is for quests that need an NPC assigned after generation (available + no NPC)
  - Characters must have `status: 'active'` to be found by `getActiveNPCs()` in side-quest-generator
  - `QUEST_GIVING_OCCUPATIONS` is case-sensitive (e.g., 'Baker' not 'baker')
---

## 2026-04-03 - Audit: Quest Reward and Progression Flow (comprehensive test suite)
- Created `shared/__tests__/quest-reward-progression-audit.test.ts` — 45 tests covering all 7 audit areas:
  1. Quest completion detection (objective completion → quest_completed callback)
  2. Reward distribution (gold, items, skills via computeSkillRewards/applySkillRewards, chain bonus XP)
  3. Quest chain progression (linear prerequisites, getNextQuestInChain, checkChainCompletion, encodeChainMeta/extractChainMeta roundtrip)
  4. Prerequisite quest unlocking (single/multiple prereqs, diamond dependency, partial prereqs blocked)
  5. Guild quest tier advancement (getNextQuestForGuild, receiveNextQuest, hasJoinedGuild, pending quest blocking)
  6. Main quest chapter transitions (QUEST_TYPE_TO_OBJECTIVE_MAP coverage, CEFR gating, isChapterComplete, initial state)
  7. Reward integration consistency (full pipeline, chain bonus stacking, idempotent tracking, difficulty scaling)
- **Audit verdict**: All reward types (XP, gold, items, skills, vocabulary category unlocks) are properly granted on quest completion. Prerequisite unlocking works server-side via findQuestsToUnlock. Chain progression works both client-side (QuestCompletionManager.handleChainProgression) and server-side (QuestChainManager.checkChainCompletion). Guild quest advancement and main quest chapter transitions are wired correctly.
- Files changed:
  - `shared/__tests__/quest-reward-progression-audit.test.ts` — new comprehensive audit test suite (45 tests)
- **Learnings for future iterations:**
  - GuildQuestManager is stateless (no constructor args) — methods take (guildId, allQuests) as params
  - QuestChainManager requires a storage provider in constructor; createQuestChain is async
  - CEFR levels must be uppercase ('A1', 'B2') — meetsChapterCefrRequirement uses indexOf on uppercase array
  - encodeChainMeta/extractChainMeta are standalone exported functions, not static methods on QuestChainManager
---

## 2026-04-03 - E2E Quest Completion Smoke Tests
- Created `shared/__tests__/quest-e2e-completion-flow.test.ts` with 27 tests covering 8 E2E scenarios:
  1. **Single quest completion → rewards**: multi-objective quest through full reward pipeline (items, skills, XP)
  2. **Ordered objectives**: sequential order enforcement before quest completion
  3. **Vocabulary unlock flow**: quest completion → category extraction → apply unlocks
  4. **Prerequisite unlocking**: completing quests auto-unlocks dependent quests (single/multi/chain)
  5. **Quest chain completion**: encode/extract chain metadata, bonus XP application, full 3-quest chain flow
  6. **Skill reward computation**: all quest types, difficulty multipliers, explicit overrides, accumulation
  7. **Vocabulary category unlocking**: initial categories, extraction, deduplication, invalid categories
  8. **Multi-quest concurrent tracking**: single event completing multiple quests, NPC/item filtering
  9. **Full new player scenario**: arrival quest → rewards → vocab unlock → prerequisite unlock
  10. **Save/load serialization**: partial objective progress persists across engine instances
- All 27 tests passing, 0 typecheck errors in new file
- Files changed:
  - `shared/__tests__/quest-e2e-completion-flow.test.ts` — new E2E smoke test file
- **Learnings for future iterations:**
  - QuestCompletionEngine, quest-completion-rewards, quest-skill-rewards, vocabulary-category-unlock, and quest-chain-manager are all independently testable pure modules
  - Chain metadata is stored in quest tags as `chain_meta:` prefixed JSON — use `encodeChainMeta`/`extractChainMeta`
  - `serializeObjectiveStates`/`restoreObjectiveStates` on QCE preserves partial progress (e.g., collectedCount)
  - Single events can complete objectives across multiple active quests simultaneously
---

## 2026-04-03 - Audit: Quest Objective Feasibility
- Created `shared/quest-objective-feasibility-auditor.ts` — comprehensive auditor that checks:
  1. Every canonical objective type (46) has a QuestCompletionEngine handler
  2. Every canonical type has a completion event mapping (OBJECTIVE_COMPLETION_EVENT_MAP)
  3. Every canonical type has an action requirement mapping (OBJECTIVE_ACTION_MAP)
  4. Internal objective types (arrival_reading, arrival_listening, arrival_conversation, arrival_initiate_conversation, arrival_writing, complete_assessment) are tracked
  5. Quest data has no unresolved placeholder IDs ({npcId}, {{npc}}, PLACEHOLDER, etc.)
  6. Dead/stale entries in completion event map are flagged
- **Audit results**: All 46 canonical objective types have 100% handler and event mapping coverage
- **Tests**: 31 tests in `shared/__tests__/quest-objective-feasibility-auditor.test.ts` covering:
  - Type coverage validation (handler, event mapping, action mapping)
  - Unknown type detection and normalization
  - Placeholder detection (npcId, itemName, locationName, etc.)
  - Full audit report generation and formatting
  - Quest seed library validation (all seeds are feasible)
  - Cross-system validation (QUEST_ACTION_MAPPINGS, ACTION_MAPPED_OBJECTIVE_TYPES)
- Fixed pre-existing test failures in `shared/__tests__/quest-objective-feasibility-audit.test.ts` — the old test loaded from JSON data files (guild-quests.json, seed-quests.json) that no longer exist; updated assertions to handle missing files gracefully
- Files changed:
  - `shared/quest-objective-feasibility-auditor.ts` — new auditor module
  - `shared/__tests__/quest-objective-feasibility-auditor.test.ts` — 31 tests
  - `shared/__tests__/quest-objective-feasibility-audit.test.ts` — fixed broken assertions
- **Learnings for future iterations:**
  - Quest data sources are primarily TypeScript (quest-seed-library.ts, generators) not JSON files — load from TS exports for testing
  - QCE has 6 internal objective types (arrival_*, complete_assessment) not in the canonical set but used for assessment/onboarding quests
  - `forEachObjective` calls in QCE are the ground truth for which types are handled — grep for them to build the handler registry
  - The normalization map in quest-objective-types.ts handles 150+ aliases for AI-generated objective types
---

## 2026-04-03 - Audit: Minimap Quest Markers and Waypoint Guidance
- **Audit findings**: 4 critical issues in quest guidance systems:
  1. `DynamicQuestWaypointDirector` and `QuestWaypointManager` existed but were NOT instantiated or used in BabylonGame.ts — no 3D waypoints or dynamic position resolution was active
  2. `extractObjectiveMarkers()` silently skipped objectives without explicit positions — no fallback to dynamic resolution, so many quest objectives had no minimap markers
  3. Distance fading used different breakpoints between `QuestWaypointManager` (max 0.6 alpha) and `DynamicQuestWaypointDirector` (different curve) — inconsistent visual behavior
  4. Missing objective types in color map: language types (use_vocabulary, pronunciation_check, etc.), physical action types (farm_plant, fish, mine, etc.), observation types (observe_activity, eavesdrop) had no dedicated color categories
- **Fixes applied**:
  - Enhanced `extractObjectiveMarkers()` to accept optional `resolvedPositions` map from DynamicQuestWaypointDirector — objectives without explicit positions now get markers via dynamic resolution
  - Added 2 new color categories: blue (#2196F3) for language/education types (9 types), amber (#FFC107) for physical action types (9 types); added `eavesdrop` to NPC interaction green category
  - Created `shared/game-engine/logic/waypointFading.ts` with shared `computeWaypointAlpha()` function — unified fading breakpoints between QuestWaypointManager and DynamicQuestWaypointDirector
  - Integrated DynamicQuestWaypointDirector and QuestWaypointManager into BabylonGame.ts:
    - Instantiated both in scene setup alongside QuestIndicatorManager
    - Resolved waypoints in `updateQuestIndicators()` using building/NPC data already assembled there
    - Cached resolved positions in `_resolvedWaypointPositions` for minimap marker extraction
    - Added 3D waypoint lifecycle management: create for new objectives, remove stale on completion/status change
    - Added distance fading update per indicator cycle
    - Added dispose/cleanup in BabylonGame.dispose()
- **Tests added**: 83 tests in `shared/__tests__/minimap-quest-markers-audit.test.ts` covering:
  - extractObjectiveMarkers: position priority, quest-level fallback, dynamic resolution fallback, multi-objective quests, marker cleanup on completion/status change
  - Color map: all 9 color categories verified (52 objective types total)
  - Shape map: diamond for location types, circle for others
  - DynamicQuestWaypointDirector: explicit position, NPC lookup, building map fallback, business-type matching, nearest NPC for conversation objectives, quest giver fallback, compass data
  - computeWaypointAlpha: arrival hiding, fade-in, full visibility, fade-out, dim far distance
  - ID consistency between minimap markers and director output
- Files changed:
  - `shared/game-engine/logic/QuestMinimapMarkers.ts` — added dynamic position fallback param, 2 new color categories, eavesdrop to NPC category
  - `shared/game-engine/logic/waypointFading.ts` — new shared fading function
  - `shared/game-engine/logic/DynamicQuestWaypointDirector.ts` — delegated fading to shared function
  - `shared/game-engine/rendering/QuestWaypointManager.ts` — delegated fading to shared function (scaled to beam max alpha)
  - `shared/game-engine/rendering/BabylonGame.ts` — integrated Director + WaypointManager, wired to minimap + 3D waypoints + cleanup
  - `shared/__tests__/minimap-quest-markers-audit.test.ts` — 83 audit tests
- **Learnings for future iterations:**
  - BabylonGame.ts `updateQuestIndicators()` already builds directorBuildingData, npcBuildingMap, and npcPositions — reuse these for any quest spatial logic
  - The `_resolvedWaypointPositions` cache bridges the async indicator update with the per-frame minimap update
  - system-contracts QuestObjective is minimal (no type/position fields) — the real runtime quests have these via `(quest as any)` casts; pre-existing TS errors
  - QuestWaypointManager beam alpha caps at 0.6 (translucent beams) while compass/HUD alpha goes to 1.0 — intentionally different scales using shared normalized curve
---

## 2026-04-03 - Audit: Tutorial System — Can a New Player Complete the Tutorial?
- **Audit findings**: Tutorial system had 4 bugs preventing new players from completing the tutorial:
  1. `tut_inventory` objective had no event listener — player opens inventory but nothing completes the step
  2. `tut_quest_log` objective had no event listener — same issue for quest log
  3. `completeCurrentObjective()` didn't skip already-completed objectives after `skipNonAssessmentObjectives()`, so assessment-only flow got stuck
  4. `onboarding.test.ts` had stale assertions referencing old phase IDs (`arrival_conversational`, `arrival_visual`) that don't exist in actual code
- **Fixes applied**:
  - Added `inventory_opened` and `quest_log_opened` event types to `GameEventBus`
  - Wired `inventory_opened` → `tut_inventory` and `quest_log_opened` → `tut_quest_log` listeners in `TutorialQuestSystem.wireEventListeners()`
  - Fixed `completeCurrentObjective()` to skip over already-completed objectives when advancing
  - Fixed all stale phase ID assertions in `onboarding.test.ts` to match actual code (`arrival_reading`, `arrival_writing`, `arrival_listening`, `arrival_conversation`)
- **Tests added**: 25 tests in `shared/__tests__/tutorial-system-audit.test.ts` covering:
  - Full end-to-end tutorial completion (with and without assessment)
  - Every objective has a mapped event that can complete it
  - Event listener wiring for inventory_opened and quest_log_opened
  - Out-of-order and post-dispose event handling
  - OnboardingManager 10-step progression, skip, and prerequisite validation
  - Assessment phase alignment between TutorialQuestSystem and AssessmentEngine
  - skipNonAssessmentObjectives followed by assessment-only completion
  - OnboardingLauncher helper functions (isFirstPlaythrough, isLanguageLearningWorld, getTargetLanguage)
  - Progress tracking accuracy and event emission
- Files changed:
  - `shared/game-engine/logic/GameEventBus.ts` — added `inventory_opened` and `quest_log_opened` event types
  - `shared/game-engine/logic/TutorialQuestSystem.ts` — added 2 event listeners, fixed objective advancement logic
  - `shared/__tests__/onboarding.test.ts` — fixed 3 test assertions with stale phase IDs
  - `shared/__tests__/tutorial-system-audit.test.ts` — new 25-test audit suite
- **Learnings for future iterations:**
  - TutorialQuestSystem objectives are strictly sequential — `completeObjectiveById` only works on the current objective
  - `skipNonAssessmentObjectives()` marks non-assessment objectives completed but doesn't update `currentObjectiveIndex` past them — the advancement logic must skip over them
  - OnboardingManager and TutorialQuestSystem are two parallel systems — OnboardingManager orchestrates the broader 10-step flow, TutorialQuestSystem tracks the HUD objectives
  - The writing assessment phase is managed by OnboardingManager, not TutorialQuestSystem — by design, since it doesn't need a HUD objective
  - `GameEventBus` event types must be added before they can be used in listeners — no runtime event registration
---

## 2026-04-01 - Audit: Quest Reward and Progression Flow
- **Audit findings**: Quest completion endpoint had 3 gaps in reward/progression flow:
  1. Item rewards defined on quests (`itemRewards`) were never persisted to player inventory on the server side
  2. Completing a quest did NOT auto-unlock dependent quests (those with `prerequisiteQuestIds` pointing to it)
  3. Quest chain completion bonus XP was calculated and returned in API response but never applied to player's actual XP total
- **Fixes applied**:
  - Created `shared/quests/quest-completion-rewards.ts` with 3 pure functions: `grantItemRewards()`, `findQuestsToUnlock()`, `applyChainBonusXP()`
  - Updated quest completion endpoint (`server/routes.ts`) to:
    - Persist item rewards to `playerProgress.saveData.inventory` (merging with existing items)
    - Scan all world quests for `prerequisiteQuestIds` containing the completed quest, unlock when all prereqs met
    - Apply chain completion bonus XP to `playerProgress.experience` via `applyChainBonusXP()`
  - Added `itemRewards` and `questsUnlocked` fields to API response
- **Tests added**: 19 tests covering all 3 functions (item granting, prerequisite unlocking, chain bonus XP)
- Files changed:
  - `shared/quests/quest-completion-rewards.ts` — new shared module with pure testable functions
  - `shared/__tests__/quest-completion-rewards.test.ts` — 19 tests for the 3 functions
  - `server/routes.ts` — quest completion endpoint updated with all 3 fixes
- **Learnings for future iterations:**
  - Item rewards are stored in `playerProgress.saveData.inventory` (jsonb), not as separate DB entities
  - `prerequisiteQuestIds` is an array of quest IDs on the quest schema — completing one quest should check all world quests for dependencies
  - Chain completion bonus XP comes from `extractChainMeta()` in quest-chain-manager — encoded in quest tags
  - The quest completion endpoint is at line ~9661 in routes.ts and handles: XP/gold, skill rewards, item rewards, vocab unlocks, prerequisite unlocking, depletion, chain completion
---

## 2026-04-01 - Audit: Conversation Action Registration → Quest Objective Completion
- Created comprehensive audit test suite: `shared/__tests__/ConversationActionQuestCompletion.test.ts` (51 tests)
- Verified all 5 conversational action types map to correct objective types:
  - asked_about_topic → asked_about_topic
  - used_target_language → used_target_language + arrival_writing
  - answered_question → answered_question
  - requested_information → requested_information + ask_for_directions
  - made_introduction → made_introduction + introduce_self
- Verified arrival_initiate_conversation auto-completes on any conversational action
- Verified NPC filtering (objectives with npcId skip non-matching NPCs)
- Verified topic matching for asked_about_topic objectives with targetWords
- Verified requiredCount thresholds (incremental progress, default 1)
- Verified conversation_turn_counted → arrival_conversation completion (meaningful turns threshold)
- Verified LLM evaluation path: conversation_goal_evaluated with confidence >= 0.7 completes objectives
- Verified ConversationQuestBridge: getObjectivesForEvaluation filters by type/NPC/completion/quest status, limits to 5
- Verified ConversationQuestBridge: processEvaluations fires trackEvent + GameEventBus emit
- Verified end-to-end: ConversationalActionDetector → QuestCompletionEngine for all 5 action types + turn counting
- Verified multiple objectives from single message (e.g., introduction + initiate_conversation)
- Verified dependency ordering blocks locked conversation objectives
- All 51 tests passing, 0 typecheck errors
- Files changed:
  - `shared/__tests__/ConversationActionQuestCompletion.test.ts` — new audit test file
- **Learnings for future iterations:**
  - ConversationalActionDetector produces actions; BabylonGame dispatches them as `conversational_action` events to QuestCompletionEngine
  - Two parallel paths for conversation quest completion: (1) pattern-based via ConversationalActionDetector, (2) LLM-based via ConversationQuestBridge
  - LLM path uses confidence threshold of 0.7 — both in ConversationQuestBridge.processEvaluations and QuestCompletionEngine.trackConversationGoalResult
  - arrival_initiate_conversation is a special auto-complete: fires on ANY conversational action (proves conversation was started)
  - Test files must go in `shared/__tests__/` (not `shared/game-engine/rendering/__tests__/`) to match vitest include patterns

## 2026-04-01 - Audit: Guild Quest Assignment
- **Audit findings**: Guild quest system had 3 bugs preventing players from getting quests from guild masters:
  1. Migration 048 set all guild quest statuses to 'available' — only Tier 0 should be; rest should be 'unavailable' until received from guild master
  2. No guild master NPC was ever linked to guild quests (`assignedByCharacterId` never set)
  3. `GameQuestManager.receiveGuildQuest()` called `GuildQuestManager.receiveNextQuest()` with wrong argument order and extra `playerName` param
- **Fixes applied**:
  - Added `findGuildMasterNpc()` to `GuildQuestManager` — matches NPC by occupation against guild's `guildMasterOccupation` (case-insensitive)
  - Updated `receiveNextQuest()` to accept optional `characters` array and assign guild master NPC to quest
  - Fixed `receive-quest` server endpoint to fetch characters and pass them to `receiveNextQuest()`
  - Fixed `GameQuestManager.receiveGuildQuest()` argument order and removed invalid `playerName` param
  - Fixed `GameQuestManager.getGuildProgress()` to not pass extra `playerName` arg
  - Fixed migration 048 to use `'unavailable'` status for non-tier-0 quests
  - Created migration 054 to fix existing guild quest data (status + guild master assignment)
- **Tests added**: 8 new tests for guild master NPC lookup and assignment
- Files changed:
  - `shared/quests/guild-quest-manager.ts` — added `findGuildMasterNpc()`, updated `receiveNextQuest()` signature
  - `shared/game-engine/logic/GameQuestManager.ts` — fixed argument order in `receiveGuildQuest()` and `getGuildProgress()`
  - `server/routes.ts` — updated receive-quest endpoint to fetch characters and persist guild master assignment
  - `server/migrations/048-seed-guild-quests.ts` — fixed initial quest status for non-tier-0 quests
  - `server/migrations/054-fix-guild-quest-status-and-masters.ts` — new migration to fix existing data
  - `shared/__tests__/quest-e2e-smoke.test.ts` — 8 new tests for guild master assignment
- **Learnings for future iterations:**
  - Guild master NPCs are identified by occupation matching against `guildMasterOccupation` in guild definitions
  - `GameQuestManager` (client-side) wraps `GuildQuestManager` (shared) — always verify argument order matches
  - Migration 048 had a subtle bug: all quests were 'available' making the receive-from-guild-master flow unnecessary
  - NPC quest guidance system uses `assignedByCharacterId` to detect quest givers — without it, guild masters won't guide conversations
---

## 2026-03-29 - US-001
- Cleaned whitespace in action converter: removed 3 unconditional blank line pushes (lines 104, 133, 159) that caused double/triple blanks when prereqs, triggers, effects, and side effects were all empty
- Added post-processing regex to collapse consecutive blank lines (`\n{3,}` → `\n\n`) and trim trailing blanks
- Created migration 041-clean-action-prolog-whitespace.ts to re-run converter on all base actions
- Files changed:
  - `shared/prolog/action-converter.ts` — removed unconditional blank line pushes, added output normalization
  - `server/migrations/041-clean-action-prolog-whitespace.ts` — new migration (idempotent, reports updated vs unchanged)
- **Learnings for future iterations:**
  - The converter pushes lines into an array then joins — easier to post-process the joined string than manage blank lines inline
  - Pre-existing typecheck errors are widespread; verify only your changed files with `grep` on tsc output
  - Migration pattern: copy from 039, adjust converter call params — storage.getBaseActions() + storage.updateAction()
  - tsx imports use `.ts` extension (not `.js`) when running via `npx tsx -e`
---

## 2026-03-29 - US-002
- Verified existing `shared/prolog/gameplay-predicates.ts` meets all acceptance criteria
- 18 player state predicates, 6 NPC predicates, 8 world state predicates, 4 location predicates
- Helper functions: getPredicatesByCategory(), getPredicateByName(), getMinimumViableTemplate()
- MVT (Minimum Viable Template) = health, energy, speaks_language, gold, at_location, occupation, age
- Files changed:
  - `shared/prolog/gameplay-predicates.ts` — already implemented (committed as new tracked file)
  - `tasks/prd.json` — marked US-002 passes: true
- **Learnings for future iterations:**
  - The file was already written but untracked — check `git status` for `??` files before implementing
  - Types exported: GameplayPredicate, PredicateArg, SyncDirection, PredicateCategory
  - This file is separate from `predicate-schema.ts` (which maps Mongo collections to Prolog); gameplay-predicates handles runtime gameplay truth
---

## 2026-03-29 - US-003
- Created `shared/prolog/helper-predicates.ts` with HELPER_PREDICATES_PROLOG string containing:
  - cefr_level_rank/2 and cefr_gte/2 for CEFR language level comparison
  - is_weapon_type/2 for weapon classification (sword, axe, bow, staff, pistol)
  - is_tool_type/2 for tool classification (fishing_rod, pickaxe, axe, hoe)
  - skill_tier_name/2 mapping levels 1-10 to tier names (novice, apprentice, journeyman, expert, master)
  - skill_gte/3 for skill level comparison against has_skill/3
- Integrated into GamePrologEngine.initialize() — loads before NPC reasoning rules
- Updated all 4 export engine templates (Unreal .h/.cpp, Unity .cs, Godot .gd) with LoadHelperPredicates/load_helper_predicates methods
- Files changed:
  - `shared/prolog/helper-predicates.ts` — new file
  - `shared/game-engine/logic/GamePrologEngine.ts` — import + consult step
  - `server/services/game-export/unreal/templates/source/systems/PrologEngine.h` — declaration
  - `server/services/game-export/unreal/templates/source/systems/PrologEngine.cpp` — implementation
  - `server/services/game-export/unity/templates/Scripts/Systems/PrologEngine.cs` — implementation
  - `server/services/game-export/godot/templates/scripts/systems/prolog_engine.gd` — implementation
  - `shared/game-engine/drift-manifest.json` — updated hash
- **Learnings for future iterations:**
  - Drift hook blocks commits if export templates aren't updated — run `npm run drift:update` after updating templates
  - Helper predicates use `:- dynamic(item_type/2).` declaration since item_type facts are asserted at runtime
  - Export templates follow consistent pattern: string literal of Prolog rules → append to KB → parse/log
  - Pre-existing typecheck errors in GamePrologEngine.ts (QueryResult type issues) are known and unrelated
---

## 2026-03-29 - US-004
- Created `shared/prolog/action-prerequisites.ts` with PrerequisiteDefinition type and ACTION_PREREQUISITES record
- 27 combat action prerequisites defined across categories: base combat, sword, unarmed, shield, ranged, defense, magic, and animation-only
- Key patterns: inheritsFromParent=true for child actions that inherit parent prerequisites; empty prerequisites array for animation-only actions
- Files changed:
  - `shared/prolog/action-prerequisites.ts` — new file
  - `tasks/prd.json` — marked US-004 passes: true
- **Learnings for future iterations:**
  - energy/3 predicate uses (Actor, Current, Max) — energy checks need `energy(Actor, E, _), E >= N` pattern (not energy/2)
  - Action IDs found in migrations 019, 020, 032, 038 — use snake_case names matching animation clip IDs
  - alive/1 predicate already exists in tott-predicates.ts, npc-reasoning.ts — safe to reference in prerequisites
  - US-005 and US-006 will add to this same file (resource, commerce, social, language prerequisites)
---

## 2026-03-29 - US-005
- Added resource and commerce action prerequisites to `shared/prolog/action-prerequisites.ts`
- 12 resource actions: gather (base), chop_tree, mine_rock, fish, gather_herb, farm_plant, farm_water, farm_harvest, cook, craft_item, fix_repair
- 7 commerce actions: trade, buy_item, sell_item, work (base), paint, sweep, push_object
- Resource actions use at_location_type + tool requirements; commerce uses near + npc_will_trade
- Files changed:
  - `shared/prolog/action-prerequisites.ts` — added RESOURCE_PREREQUISITES and COMMERCE_PREREQUISITES sections
  - `tasks/prd.json` — marked US-005 passes: true
- **Learnings for future iterations:**
  - gather is a base action; chop_tree/mine_rock/fish/gather_herb inherit from it via inheritsFromParent: true
  - Farm actions (farm_plant, farm_water, farm_harvest) are standalone (not inheriting from gather) since farming is its own category
  - Work sub-actions (paint, sweep, push_object) inherit from work and only add energy checks
  - buy_item has gold check `gold(Actor, G), G > 0` in addition to trade prerequisites
---

## 2026-03-29 - US-006
- Added social, language, exploration, item, survival, and movement/idle/expression prerequisites to `shared/prolog/action-prerequisites.ts`
- 6 new prerequisite sections: SOCIAL (20 actions), LANGUAGE (11 actions), EXPLORATION (2), ITEM (5), SURVIVAL (3), ANIMATION_ONLY (10)
- Social: talk_to_npc base with 15 children inheriting near(5); give_gift, steal, eavesdrop, request_quest standalone
- Language: CEFR level gates via cefr_gte — a1 for order_food, a2 for haggle/describe, b1 for teach_vocabulary
- Files changed:
  - `shared/prolog/action-prerequisites.ts` — added 6 new prerequisite sections (368 lines)
  - `tasks/prd.json` — marked US-006 passes: true
- **Learnings for future iterations:**
  - Social child actions use empty prerequisites + inheritsFromParent: true to inherit parent's near() check
  - Language actions combine speaks_language with cefr_gte in a single goal string for proper Prolog variable binding
  - rest/sleep use `E >= 0` energy check (always succeeds if energy exists) — acts as existence check
  - Movement/idle/expression actions have empty prerequisites and inheritsFromParent: false (truly no requirements)
---

## 2026-03-29 - US-007
- Created `shared/prolog/action-effects.ts` with EffectDefinition type and ACTION_EFFECTS record
- 12 resource effects: gather (base energy), chop_tree, mine_rock, fish, gather_herb, farm_plant, farm_water, farm_harvest, cook, craft_item, fix_repair
- 7 commerce effects: trade, buy_item, sell_item, work, paint, sweep, push_object
- Effect patterns: assert(has_item(...)) for item creation, modify_energy for costs, modify_skill_xp for skill gains, modify_gold for currency transfer
- Files changed:
  - `shared/prolog/action-effects.ts` — new file
  - `tasks/prd.json` — marked US-007 passes: true
- **Learnings for future iterations:**
  - Effect terms use modify_energy/modify_gold/modify_skill_xp helper predicates (to be implemented in runtime)
  - buy_item/sell_item use symmetric gold transfer: modify_gold(Actor, -Price) + modify_gold(Target, Price)
  - US-008 will add combat, social, and language effects to this same file
  - File follows same structure as action-prerequisites.ts: category-specific Records merged into combined export
---

## 2026-03-29 - US-008
- Added combat, social, language, exploration, item, survival, and animation-only effects to `shared/prolog/action-effects.ts`
- 7 new effect sections: COMBAT (17 actions), SOCIAL (20 actions), LANGUAGE (13 actions), EXPLORATION (2), ITEM (5), SURVIVAL (3), ANIMATION_ONLY (19 actions)
- Combat: damage via modify_health(Target, -N), energy cost, combat XP; defense actions cost energy only
- Social: disposition changes via modify_disposition(Target, Actor, N); give_gift retracts item; steal asserts stolen item + stealth XP
- Language: XP gains scaled by difficulty (5 for basic, 10-15 for practice, 20 for teaching); examine_object/point_and_name/read_sign assert knows_vocabulary
- Files changed:
  - `shared/prolog/action-effects.ts` — added 7 new effect sections (477 lines)
  - `tasks/prd.json` — marked US-008 passes: true
- **Learnings for future iterations:**
  - Social effects use modify_disposition/3 helper (to be implemented in runtime) instead of direct npc_disposition manipulation
  - Language effects use modify_xp(Actor, language, N) — language XP is a separate skill tracked alongside CEFR levels
  - Animation-only actions (idle, walk, run, combat reactions, zombie_*) have empty effects arrays
  - throw_projectile is the only combat action that consumes an item (retract projectile)
---

## 2026-03-29 - US-009
- Updated `convertActionToProlog()` to accept optional `ConvertOptions` parameter with `prerequisites: string[]`, `effects: string[]`, `inheritsFromParent: boolean`
- New-style prerequisites: when provided, used directly as can_perform goals (no default energy check — prereqs already include energy checks where needed)
- Parent inheritance: when `inheritsFromParent=true` and `parentAction` is set, looks up parent from `ACTION_PREREQUISITES` and prepends parent's prerequisites
- Effects: emits `action_effect(actionId, (effect)).` for each effect string
- Animation-only actions with no prerequisites get unconditional `can_perform(_, actionId).`
- Legacy path preserved: when no options provided, old JSON-based conversion still works
- Also fixed legacy energy check from `energy(Actor, E)` (arity 2) to `energy(Actor, E, _)` (arity 3) to match gameplay-predicates.ts
- Files changed:
  - `shared/prolog/action-converter.ts` — added ConvertOptions, new-style prereq/effect handling, parent resolution, legacy energy/3 fix
  - `tasks/prd.json` — marked US-009 passes: true
- **Learnings for future iterations:**
  - The converter wraps each prereq/effect in parens: `action_prerequisite(id, (goal)).` — this ensures compound goals parse correctly in Prolog
  - Parent resolution imports ACTION_PREREQUISITES directly — no need for caller to resolve
  - Actions with empty prerequisites and `inheritsFromParent=false` get `can_perform(_, id).` (wildcard Actor)
  - Content validator passes with 0 errors for all new-style output; animation-only actions still get action_effect/2 warning (expected)
  - The `energy/2` → `energy/3` fix also applies to the batch converter's dynamic declarations
---

## 2026-03-29 - US-010
- Created migration `server/migrations/042-action-prerequisites-effects.ts` to regenerate all 133 base actions with new-style prerequisites and effects
- Migration reads ACTION_PREREQUISITES and ACTION_EFFECTS by action name, passes ConvertOptions to convertActionToProlog()
- Actions without prerequisite/effect definitions fall back to legacy conversion (no options passed)
- Reports counts: updated, unchanged, errors, and actions with prereqs/effects
- Files changed:
  - `server/migrations/042-action-prerequisites-effects.ts` — new migration
  - `tasks/prd.json` — marked US-010 passes: true
- **Learnings for future iterations:**
  - Migration numbering: 040 (consolidate collections) and 041 (whitespace cleanup) already existed — new migration is 042
  - Pattern: pass `undefined` for options when no prereqs/effects exist to preserve legacy behavior
  - `storage.getBaseActions()` returns all base actions; match by `action.name` against prerequisite/effect records
  - The ConvertOptions only needs `prerequisites`, `effects`, `inheritsFromParent` — parent resolution happens inside the converter
---

## 2026-03-29 - US-011
- Created `shared/game-engine/logic/GameTruthSync.ts` — new class that syncs equipment and inventory events to canonical Prolog gameplay predicates
- Uses `onAny` + switch pattern (same as GamePrologEngine) to handle events with proper TypeScript type narrowing
- Equipment: item_equipped asserts has_equipped/3, retracts previous slot occupant; item_unequipped retracts
- Inventory: item_collected/item_crafted call addItem (assert has_item/3 with quantity tracking); item_dropped calls removeItem (retract or decrement)
- Purchases: item_purchased adds item + deducts gold via modifyGold helper
- Gold tracking: initializeGold() for setup, modifyGold() for retract-old/assert-new pattern
- Local state tracking: itemQuantities Map for quantity, equippedSlots Map for slot→itemId
- Files changed:
  - `shared/game-engine/logic/GameTruthSync.ts` — new file (204 lines)
  - `tasks/prd.json` — marked US-011 passes: true
- **Learnings for future iterations:**
  - GameEventBus typed `on()` doesn't work well with extracted handler functions due to union type issues — use `onAny()` + switch (same pattern as GamePrologEngine)
  - GameTruthSync uses has_equipped/3 and has_item/3 (canonical gameplay predicates); GamePrologEngine uses equipped/3 and has/2 (quest-tracking predicates) — they coexist
  - TauPrologEngine.retractFact() may throw if fact doesn't exist — always wrap in try/catch
  - Constructor takes TauPrologEngine (not raw tau-prolog session) — all operations go through the async wrapper
  - US-012 will extend this class with location, health, energy, and time sync
---

## 2026-03-29 - US-012
- Extended GameTruthSync with location, health, energy, time, vocabulary, and language sync
- Location: location_visited → at_location/2 + location_discovered/1; settlement_entered → at_location/2 + at_location_type(town)
- Health/Energy: public modifyHealth(delta)/modifyEnergy(delta) + initializeHealth/initializeEnergy methods; energy auto-deducted from action_executed events with energyCost
- Time: hour_changed → time_of_day/1; day_changed → day_number/1; initializeTime for setup
- Vocabulary: vocabulary_used (correct only) → knows_vocabulary/3 for all known languages
- Language: assessment_completed with cefrLevel → updates speaks_language/3 for target language; initializeLanguage for setup
- Added local state tracking: currentLocation, currentLocationType, currentHealth, currentEnergy, currentHour, currentDay, knownVocabulary (Map<string, Set>), languageLevels (Map<string, string>)
- Files changed:
  - `shared/game-engine/logic/GameTruthSync.ts` — added 225 lines: 7 new event handlers, 6 public initialization/modification methods
  - `tasks/prd.json` — marked US-012 passes: true
- **Learnings for future iterations:**
  - No explicit health_changed/energy_changed events exist in GameEventBus — use action_executed.energyCost for auto-deduction, public modifyHealth/modifyEnergy for direct changes
  - Map iteration with `for...of` triggers TS2802 when target < es2015 — use `Array.from(map.keys())` or `Array.from(map.entries())` instead
  - vocabulary_used event only has `word` and `correct` — no language/category info; category defaults to 'general', language derived from tracked languageLevels
  - assessment_completed has optional cefrLevel — skip if not present; updates first non-english language (target language)
  - setLocationType is public so external systems (terrain detection, building entry) can update location type
---

## 2026-03-29 - US-013
- Added serialize() and restore() methods to GameTruthSync for save/load of Prolog gameplay facts
- serialize() filters facts through GAMEPLAY_PREDICATES names, returns JSON array of {predicate, args}
- restore() asserts facts into engine via assertFacts() batch, then rebuilds all local state tracking maps (itemQuantities, equippedSlots, health, energy, gold, location, time, vocabulary, languages)
- Added SerializedFact type and parseFactString/parseArg helpers for parsing Prolog fact strings to JSON
- Integrated with save pipeline: added prologFacts field to GameSaveState, GameStateSource.getPrologFacts(), GameStateTarget.restorePrologFacts()
- WorldStateManager: captureState includes prologFacts, applyState calls restorePrologFacts, added to SUBSYSTEM_KEYS and computeDiff
- SaveConflictResolver: prologFacts included in subsystem merge strategy (local-if-changed, else server)
- Updated drift-manifest.json hash for types.ts (GameSaveState field addition)
- Files changed:
  - `shared/game-engine/logic/GameTruthSync.ts` — serialize/restore methods, SerializedFact type, fact parsing helpers
  - `shared/game-engine/types.ts` — prologFacts field on GameSaveState
  - `client/src/components/3DGame/WorldStateManager.ts` — save/load integration, SUBSYSTEM_KEYS, computeDiff
  - `shared/game-engine/logic/SaveConflictResolver.ts` — added prologFacts to subsystem merge list
  - `shared/game-engine/drift-manifest.json` — updated hash
- **Learnings for future iterations:**
  - TauPrologEngine.getAllFacts() returns facts as strings with trailing periods — strip before parsing
  - Export templates don't reference GameSaveState, so types.ts changes to save-state fields cause false-positive drift warnings — just run drift:update
  - parseFactString handles nested parens in args for compound terms
  - restore() rebuilds local state from facts to keep Maps/tracking variables in sync with engine state
  - SaveConflictResolver uses subsystem merge strategy for new fields: local-if-changed-from-base, else server
---

## 2026-03-29 - US-014
- Added `initializeNpcTruths(characters: NpcCharacterData[])` method to GameTruthSync
- Exports `NpcCharacterData` interface with fields: id, occupation, currentLocation, age, birthYear, skills, personality, role, availableQuests
- For each NPC asserts: person/1, occupation/2, npc_occupation/2, at_location/2, age/2, has_skill/3, personality/3, health/3, energy/3
- Merchant detection via static Sets of known merchant occupations (13 entries) and NPCRole merchant types (3 entries)
- Quest availability: npc_quest_available/2 from availableQuests array
- Age computed from birthYear if no explicit age field; defaults to 30 if neither
- Uses batch assertFacts() for efficiency — all NPC facts collected first, then asserted in one call
- Files changed:
  - `shared/game-engine/logic/GameTruthSync.ts` — NpcCharacterData interface, initializeNpcTruths method, MERCHANT_OCCUPATIONS/MERCHANT_ROLES static Sets
  - `tasks/prd.json` — marked US-014 passes: true
- **Learnings for future iterations:**
  - Character DB fields: occupation (text), currentLocation (text), skills (Record<string, number>), personality ({openness, conscientiousness, extroversion, agreeableness, neuroticism}: number)
  - NPCRole type includes: civilian, guard, soldier, merchant, questgiver, farmer, blacksmith, innkeeper, priest, teacher, doctor, child, elder, noble, beggar, sailor
  - Quests are linked to NPCs via quests table's assignedByCharacterId field, not a field on the character itself
  - US-015 will extend this with dynamic NPC truth updates (movement, trade state, relationships, quest state)
---

## 2026-03-29 - US-015
- Extended GameTruthSync with NPC dynamic truth update handlers for movement, relationships, quests, merchant stock, and schedules
- New event subscriptions: npc_volition_action (NPC movement), npc_relationship_changed (disposition), quest_accepted (retract quest availability)
- Schedule integration: hour_changed now calls updateNpcSchedules() which uses an external schedule provider callback to update NPC at_location and assert npc_schedule/4
- Merchant stock: trackMerchantStock on item_purchased with merchantId; public depleteMerchantStock/restoreMerchantStock methods for inventory system integration
- NPC state tracking: npcLocations Map, npcDispositions Map, npcTraders Set, knownNpcIds Set — populated during initializeNpcTruths
- Movement actions detected: travel_to_location, walk, run, enter_building → update at_location(NpcId, LocationId)
- All NPC truth changes use same retract-old/assert-new pattern as player truths
- Files changed:
  - `shared/game-engine/logic/GameTruthSync.ts` — added ~150 lines: 5 NPC tracking fields, 3 new event handlers, schedule provider, merchant stock API
  - `tasks/prd.json` — marked US-015 passes: true
- **Learnings for future iterations:**
  - item_purchased event includes optional merchantId and merchantName fields — use for merchant-specific tracking
  - npc_relationship_changed has npcId + newStrength — disposition is always toward the player (no towardId field on the event)
  - quest_accepted has optional assignedByNpcId — use to retract npc_quest_available for that specific NPC
  - Schedule provider is a callback pattern (setNpcScheduleProvider) because NPCScheduleSystem is in the rendering layer, not logic layer
  - NPC movement detection relies on action IDs matching known movement actions — extend NPC_MOVEMENT_ACTIONS Set if new movement actions are added
  - US-016 will add character_templates table and CRUD
---

## 2026-03-29 - US-016
- Added `character_templates` table to `shared/schema.ts` with fields: id, worldId (nullable for base templates), name, description, startingTruths (jsonb array of {predicate, args}), isDefault, isBase
- Added `characterCreationMode` field to `worlds` table: 'fixed' | 'archetype_select' | 'custom_create' (default 'fixed')
- Added Mongoose schema `CharacterTemplateSchema` and `CharacterTemplateModel` in `server/db/mongo-storage.ts`
- Added `CharacterTemplateDoc` interface, `docToCharacterTemplate` converter
- Added CRUD methods to `IStorage` interface and `MongoStorage`: getCharacterTemplate, getCharacterTemplates (returns base + world-specific), createCharacterTemplate, updateCharacterTemplate, deleteCharacterTemplate
- Added cascade delete: world deletion now also deletes associated character templates
- Added 5 API routes in `server/routes.ts`: GET/POST `/api/worlds/:worldId/character-templates`, GET/PUT/DELETE `/api/character-templates/:id`
- Files changed:
  - `shared/schema.ts` — character_templates table, characterCreationMode on worlds, insert schema, types
  - `server/db/storage.ts` — IStorage interface: 5 new character template methods
  - `server/db/mongo-storage.ts` — CharacterTemplateDoc, schema, model, converter, CRUD methods, cascade delete
  - `server/routes.ts` — 5 API routes with auth/permission checks
- **Learnings for future iterations:**
  - New collection pattern: Drizzle table in schema.ts → insert schema (omit id/timestamps) → types ($inferSelect + z.infer) → Mongoose schema + model + Doc interface + converter in mongo-storage.ts → IStorage methods → MongoStorage implementation → routes
  - getCharacterTemplates queries `{ $or: [{ worldId: null }, { worldId }] }` to include base templates alongside world-specific ones
  - Remember to add cascade delete in `deleteWorld()` when adding world-scoped collections
  - Auth pattern: extract token, verify with AuthService, check canEditWorld for write operations; read operations are unauthenticated
  - Pre-existing typecheck errors in mongo-storage.ts (all Doc interfaces have `incorrectly extends interface 'Document'`) — these are known and unrelated
  - US-017 will seed 3 default character templates (Explorer, Scholar, Trader)
---

## 2026-03-29 - US-017
- Created migration `server/migrations/043-seed-character-templates.ts` seeding 3 base character templates
- Explorer (13 truths): balanced stats, backpack/compass/map, exploration 2 + survival 1, English B1 + French A1, 100 gold, 100/100 health & energy
- Scholar (15 truths): language-focused, notebook/quill/dictionary, reading 3 + writing 2 + linguistics 1, English B2 + French A2, has_trait(bookworm), 50 gold, 80/80 health, 120/120 energy
- Trader (12 truths): commerce-focused, merchant_ledger/sample_goods, bargaining 3 + appraisal 2, English B1 + French A1, 200 gold, 90/90 health & energy
- All templates include MVT predicates (health, energy, speaks_language, gold, at_location, occupation, age)
- Explorer is isDefault: true
- Migration is idempotent — checks by name before creating
- Files changed:
  - `server/migrations/043-seed-character-templates.ts` — new migration
  - `tasks/prd.json` — marked US-017 passes: true
- **Learnings for future iterations:**
  - getCharacterTemplates('__none__') returns base templates (worldId: null) via the $or query — use dummy worldId to fetch base-only
  - Migration pattern for seed data: define constants, check existing by name, skip if present
  - US-018 will add CharacterTemplateEditor UI component
---

## 2026-03-29 - US-018
- Created `CharacterTemplatesHub` component with full CRUD: list, create, edit, duplicate, delete, isDefault toggle
- Left sidebar lists world templates + base templates with truth count badges and default star indicator
- Right panel shows detail view with starting truths rendered as Prolog facts, or edit form with name/description fields
- Base templates are read-only but can be duplicated to the world
- Delete requires confirmation via AlertDialog
- Added "Templates" tab to ModernNavbar tabMeta and WorldManagementTab contentItems
- Registered in home.tsx with conditional rendering for activeTab === 'templates'
- Files changed:
  - `client/src/components/templates/CharacterTemplatesHub.tsx` — new file (full CRUD component)
  - `client/src/components/ModernNavbar.tsx` — added LayoutTemplate icon import + templates tab to tabMeta
  - `client/src/components/WorldManagementTab.tsx` — added LayoutTemplate icon import + templates card to contentItems
  - `client/src/pages/home.tsx` — import CharacterTemplatesHub, conditional render for templates tab
  - `tasks/prd.json` — marked US-018 passes: true
- **Learnings for future iterations:**
  - Hub pattern: left sidebar list + right detail panel, following ItemsHub/QuestsHub layout
  - Direct fetch() for CRUD operations (not react-query mutations) — consistent with existing dialogs
  - useWorldPermissions(worldId) provides canEdit for permission gating
  - tabMeta in ModernNavbar is for breadcrumb display; contentItems in WorldManagementTab is for the card grid on the home page
  - Base templates have isBase=true and are read-only in the UI; duplicate creates a world-scoped copy
  - No browser testing tools available — manual verification needed
  - US-019 will add predicate builder within this template editor
---

## 2026-03-29 - US-019
- Created `PredicateBuilder` component with full predicate form builder
- Select dropdown populated from GAMEPLAY_PREDICATES, grouped by category (Player State, NPC State, World State, Location)
- Typed inputs for each argument based on predicate schema (atom → text, integer → number, float → number with step)
- Live preview showing formatted Prolog fact before adding
- Remove button on each truth row (hover-reveal)
- MVT validation banner: warns with yellow alert when required predicates are missing (health, energy, speaks_language, gold, at_location, occupation, age)
- Integrated into CharacterTemplatesHub: edit mode shows name/description + PredicateBuilder; detail view shows read-only PredicateBuilder
- startingTruths saved via PUT API alongside name/description on template save
- Files changed:
  - `client/src/components/templates/PredicateBuilder.tsx` — new component (170 lines)
  - `client/src/components/templates/CharacterTemplatesHub.tsx` — import PredicateBuilder, expanded edit state to include startingTruths, replaced inline truth display with PredicateBuilder
  - `tasks/prd.json` — marked US-019 passes: true
- **Learnings for future iterations:**
  - PredicateBuilder uses `@shared/prolog/gameplay-predicates` directly — no API call needed since predicate schema is shared code
  - Atom args get lowercase + underscore normalization via parseArgValue — prevents invalid Prolog atoms
  - Edit form state includes startingTruths array; the PUT body now includes it alongside name/description
  - Read-only mode reuses same PredicateBuilder with `readOnly` prop — hides add form and remove buttons
  - No browser testing tools available — manual verification needed
  - US-020 will add ActionFeasibilityTester using local tau-prolog
---

## 2026-03-29 - US-020
- Created `ActionFeasibilityTester` component with full action feasibility testing
- Template selector dropdown populated from all available templates (base + world)
- Creates local TauPrologEngine session: loads HELPER_PREDICATES_PROLOG, declares dynamic predicates for all GAMEPLAY_PREDICATES, asserts template truths as facts
- Builds can_perform rules from ACTION_PREREQUISITES with parent inheritance resolution
- Tests all 133+ actions grouped by 9 categories (combat, resource, commerce, social, language, exploration, items, survival, movement)
- Green checkmark = can_perform succeeds, red X = fails, gray dash = animation-only
- Click failed action to see which prerequisite(s) failed in detail panel
- "What's Missing?" helper: for failed actions, shows which gameplay predicates would need to be added
- Inline PredicateBuilder for modifying template truths and clicking Re-test without saving to DB
- Summary bar shows total pass/fail/animation-only counts
- Categories auto-expand when they have failures
- Integrated into CharacterTemplatesHub detail view (below Starting Truths when not editing)
- Files changed:
  - `client/src/components/templates/ActionFeasibilityTester.tsx` — new component (340 lines)
  - `client/src/components/templates/CharacterTemplatesHub.tsx` — import + render ActionFeasibilityTester in detail view
  - `tasks/prd.json` — marked US-020 passes: true
- **Learnings for future iterations:**
  - TauPrologEngine can be instantiated directly in client components — no API call needed for local testing
  - getResolvedPrerequisites() must recursively resolve parent prerequisites via ACTION_MATRIX parentAction links
  - For can_perform testing, replace Actor→player and Target→test_target in goal strings, and assert test environment facts (near, alive, location_accessible, npc_will_trade) to simulate a valid game world
  - Actions not in ACTION_PREREQUISITES but with status 'animation-only' in ACTION_MATRIX are treated as animation-only (gray dash)
  - engine.query() returns {success, bindings} — check bindings.length > 0 for actual results (success can be true with empty bindings on some engines)
  - No browser testing tools available — manual verification needed
  - US-021 will integrate MVT context into LLM conversation prompts
---

## 2026-03-29 - US-021
- Created `shared/prolog/mvt-context.ts` with `generateMVTContext()` function
- Formats serialized Prolog gameplay facts as a concise natural-language context block (~200 tokens)
- Covers: health/energy, gold, occupation, age, location, languages (with CEFR), equipped items, inventory (capped at 8), skills (capped at 6), vocabulary topics
- Integrated into `server/services/conversation/context-manager.ts`:
  - Added `prologFacts` to `gameState` parameter of `buildContext()`
  - Added `mvtContext` to `PromptParts` interface
  - Injected as `PLAYER GAME STATE:` block after player relationship, before language learning directives
- Updated `server/services/conversation/http-bridge.ts`:
  - Accepts `prologFacts` in request body
  - Passes to `buildContext()` via gameState
  - Rebuilds context when prologFacts are provided (not just on first message)
- Updated `client/src/components/3DGame/ConversationClient.ts`:
  - Added `setPrologFactsProvider()` for external systems (e.g., GameTruthSync) to supply current facts
  - Includes facts in conversation stream request body
- Files changed:
  - `shared/prolog/mvt-context.ts` — new file (generateMVTContext function)
  - `server/services/conversation/context-manager.ts` — import, PromptParts.mvtContext, gameState.prologFacts, injection into prompt
  - `server/services/conversation/http-bridge.ts` — prologFacts passthrough, context rebuild on facts
  - `client/src/components/3DGame/ConversationClient.ts` — setPrologFactsProvider, include in fetch body
- **Learnings for future iterations:**
  - Context manager builds system prompt in `buildSystemPrompt()` at line 514 of context-manager.ts — all NPC prompt sections are assembled there
  - HTTP bridge caches conversation context per session — must explicitly rebuild when game state changes (prologFacts triggers rebuild)
  - ConversationClient uses a provider callback pattern (like schedule provider in GameTruthSync) to avoid tight coupling
  - `Array.from(new Set(...))` instead of `[...new Set(...)]` to avoid TS2802 downlevelIteration errors
  - Pre-existing test file error in test-context-manager.ts (missing getSettlementsByWorld/getResidence on mock storage) — unrelated
---

## 2026-04-01 - E2E Audit: Main Quest "The Missing Writer"
- **Critical bug found and fixed**: Chapter objective `questType` values (`use_vocabulary`, `complete_conversation`, `find_text`, etc.) did not match actual database quest `questType` values (`vocabulary`, `conversation`, `reading`, etc.). This meant `recordQuestCompletion()` could never match any objective — the main quest was **completely stuck and could never progress**.
- Added `QUEST_TYPE_TO_OBJECTIVE_MAP` and `getObjectiveTypesForQuestType()` to `shared/quests/main-quest-progression.ts` — maps 15+ DB quest types to chapter objective types they can satisfy
- Updated `recordQuestCompletion()` matching logic to use the mapping instead of exact string comparison
- Rewrote and expanded `server/__tests__/main-quest-progression.test.ts` (51 tests, all passing):
  - Fixed mock storage: old test called `new MainQuestProgressionManager()` with no args (class requires storage param)
  - Fixed quest type strings: old test used `'vocabulary'`, `'collect_text'` without mapping — now uses correct DB quest types
  - Fixed stale chapter IDs: old test referenced `'ch1_arrival'`, `'ch1_collect_vocabulary'` which no longer exist
  - Added 8 new quest type mapping tests (coverage for all objective type → quest type paths)
  - Added full E2E test: completes all 6 chapters start to finish with CEFR gating at every boundary
  - Added case note generation test
  - Added social/visual_vocabulary/navigation/translation/collection quest type tests
- Files changed:
  - `shared/quests/main-quest-progression.ts` — QUEST_TYPE_TO_OBJECTIVE_MAP, getObjectiveTypesForQuestType, updated matching logic
  - `server/__tests__/main-quest-progression.test.ts` — complete rewrite with correct types and E2E coverage
- **Audit findings summary:**
  - All 9 chapter objective types now reachable via quest type mapping
  - All 6 chapters completable in sequence with correct CEFR gating (A1→A1→A2→A2→B1→B2)
  - Event bridges verified: all main quest event types (text_collected, writing_submitted, npc_relationship_changed, conversation_goal_evaluated) have working bridges
  - ConversationQuestBridge calls QCE directly (not through GameEventBus), so conversation_goal_evaluated works
  - Total XP across all chapters: 6,050 (300+500+750+1000+1500+2000)
- **Learnings for future iterations:**
  - Chapter objective `questType` is a semantic type (what the objective represents), NOT the DB quest type — always use the mapping
  - `reading` quests can satisfy `find_text`, `read_text`, OR `read_sign` objectives (in objective order within the chapter)
  - `grammar` quests can satisfy `use_vocabulary` OR `write_response` objectives
  - The `recordQuestCompletion` flow: BabylonGame.onQuestCompleted → quest.questType → API → progressionManager.recordQuestCompletion → mapping → chapter objective match
  - Pre-existing bug: `playthrough-truth-isolation.test.ts` also constructs `MainQuestProgressionManager()` with no args — same storage issue, unrelated to this change
---

## 2026-04-01 - Farming System Implementation
- Created `shared/game-engine/logic/FarmingSystem.ts` — full farming system following FishingSystem/HerbalismSystem pattern
- 6 crop types with French names and language learning data: wheat (blé), corn (maïs), potato (pomme de terre), carrot (carotte), tomato (tomate), fine herbs (herbes fines)
- Crop lifecycle: plant → water (daily, 1.5x growth bonus) → grow (over configurable days) → harvest
- Farming skill progression (levels 0-10 with cumulative XP thresholds)
- Yield bonus system: base yield + watering bonus + skill bonus
- Full serialization/restore for save/load support
- GameEventBus integration: responds to physical_action_completed (farm_plant/farm_water/farm_harvest) and day_changed events
- Emits item_collected events on harvest with crop taxonomy
- Updated action-matrix.ts: farm_plant, farm_water, farm_harvest status changed from 'partial' to 'implemented'
- Created `shared/__tests__/farming-system.test.ts` — 24 vitest tests covering planting, watering, growth, harvesting, skill progression, serialization, event bus integration, query helpers, and dispose
- Files changed:
  - `shared/game-engine/logic/FarmingSystem.ts` — new file (530 lines)
  - `shared/__tests__/farming-system.test.ts` — new file (24 tests)
  - `shared/game-engine/action-matrix.ts` — 3 status changes: partial → implemented
- **Learnings for future iterations:**
  - Use `Array.from(map.values())` for Map iteration to avoid TS2802 downlevelIteration errors
  - day_changed event uses `event.day` (not `event.newDay`) and also has `event.timestep`
  - Vitest tests go in `shared/__tests__/` (not alongside source files) — vitest.config.ts only includes `__tests__` directories
  - FishingSystem/HerbalismSystem/FarmingSystem all follow identical callback pattern: showToast + addInventoryItem + optional hasInventoryItem
  - Crop growth uses watered-previous-day pattern: watering on day N boosts growth on day N+1
---

## 2026-04-01 - Audit: Conversational Quest Completion LLM Evaluation Pipeline
- Audited the full LLM evaluation pipeline: BabylonChatPanel → server /api/conversation/metadata → buildConversationGoalPrompt → LLM → goalEvaluations → ConversationQuestBridge.processEvaluations → QuestCompletionEngine.trackConversationGoalResult
- **Critical chain verified:** All links connected end-to-end. No broken references.
- **Bugs fixed:**
  - Extracted `MIN_CONVERSATION_GOAL_CONFIDENCE = 0.7` shared constant (was hardcoded in two places: ConversationQuestBridge line 118 and QCE line 1300)
  - Added `Array.isArray()` validation in `processEvaluations()` — previously `null` could pass the truthy check
  - Added locked objective filtering in `getObjectivesForEvaluation()` — was returning objectives blocked by `dependsOn`/`order` constraints
  - Fixed `grammar_feedback` → `grammar_demonstrated` event type in BabylonChatPanel (QCE had no handler for `grammar_feedback`)
  - Fixed `translation_attempt` event field: `correct` → `isCorrect` to match QCE CompletionEvent type
  - Fixed `npc_relationship_changed` → `friendship_changed` event type + `newStrength` → `relationshipStrength` field in BabylonChatPanel to match QCE handler
  - Server now always includes `goalEvaluations` in metadata response (even if empty array) for schema consistency
- **43 new tests** covering: confidence threshold gating, input validation (null/undefined/non-array), locked objective filtering, full integration flow, GameEventBus emission, objective collection, event field alignment, QCE handler behavior
- Files changed:
  - `shared/game-engine/logic/ConversationQuestBridge.ts` — exported MIN_CONVERSATION_GOAL_CONFIDENCE, Array.isArray check, locked objective filtering, isObjectiveLocked on QuestTracker interface
  - `shared/game-engine/logic/QuestCompletionEngine.ts` — import shared constant, use MIN_CONVERSATION_GOAL_CONFIDENCE
  - `server/services/conversation/http-bridge.ts` — always include goalEvaluations in response
  - `shared/game-engine/rendering/BabylonChatPanel.ts` — fix 3 event type/field mismatches (grammar_demonstrated, isCorrect, friendship_changed)
  - `shared/__tests__/conversation-quest-pipeline-audit.test.ts` — new test file (43 tests)
- **Learnings for future iterations:**
  - The confidence threshold 0.7 is used in TWO places — always use the shared constant `MIN_CONVERSATION_GOAL_CONFIDENCE` from ConversationQuestBridge.ts
  - BabylonChatPanel emits events via `(this._gameEventBus as any).emit()` — field names MUST match CompletionEvent union type in QCE exactly, or the handler silently reads `undefined`
  - QCE's `forEachObjective()` already filters locked objectives, but `getObjectivesForEvaluation()` in the bridge did NOT — any code querying quests directly must check locking
  - The server's `buildConversationGoalPrompt()` doesn't pass objective-specific fields (targetWords, npcId) — a future improvement to increase evaluation accuracy
  - Pre-existing typecheck errors in BabylonChatPanel (missing module declarations for actions, RelationshipManager, LanguageProgressTracker) are known and unrelated
---

## 2026-04-01 - Audit: Item Usage and Placement
- **Audit findings:**
  - Container spawning (ContainerSpawnSystem) works correctly: exterior containers spawn near buildings, interior containers from furniture meshes, loot is context-sensitive per business type, quest items injected at 30% chance
  - Container interaction pipeline works: clicking mesh triggers `__open_container__` action → `handleContainerInteraction()` → `openContainerPanel()` shows BabylonContainerPanel with Take/Examine/Place buttons
  - Item use/equip/consume handlers (`handleUseItem`, `handleEquipItem`, `handleUnequipItem`, `handleDropItem`) all emit correct events and apply effects
  - **CRITICAL GAP FOUND**: `item_used`, `item_equipped`, `item_dropped`, `item_purchased`, `item_crafted` events were emitted on GameEventBus but never forwarded to `QuestCompletionEngine.handleGameEvent()` — meaning `use_item`, `equip_item`, `drop_item`, `buy_item`, and `craft_item` quest objectives via the generic matcher could never complete
- **Fixes applied:**
  1. Wired 5 missing event bridges in BabylonGame.ts (lines ~3910-3945): item_used, item_equipped, item_dropped, item_purchased, item_crafted → QuestCompletionEngine.handleGameEvent()
  2. Fixed operator precedence bug in `handleUseItem`: `item.type === 'document' || item.type === 'collectible' && item.category === 'document'` → added parens around the `&&` clause
  3. Added missing `action_executed` event emission in `handleUnequipItem` for consistency with `handleEquipItem`
- Files changed:
  - `shared/game-engine/rendering/BabylonGame.ts` — 3 fixes (event bridges, operator precedence, unequip event)
  - `shared/__tests__/item-event-bridges.test.ts` — new test file (11 tests: use_item, equip_item, drop_item, buy_item, craft_item via generic matcher, edge cases)
- **Learnings for future iterations:**
  - `QuestCompletionEngine` has TWO event processing paths: `trackEvent()` with a manual switch statement, and `handleGameEvent()` using the generic `QUEST_ACTION_MAPPINGS` from `quest-action-mapping.ts`. New event types should use `handleGameEvent()` (the generic matcher) to avoid duplicating logic
  - The `quest-action-mapping.ts` catalog had correct mappings for use_item/equip_item/drop_item/buy_item but they were dead code until wired
  - Vitest test files must be in `shared/__tests__/` (not `shared/game-engine/rendering/__tests__/`) per vitest.config.ts include patterns
  - Pre-existing typecheck errors (828) are known — grep for your specific files to verify no new errors
---

## 2026-04-01 - Audit: NPC Conversation Initiation
- Audited NPCInitiatedConversationController end-to-end: approach evaluation, greeting, player acceptance, cooldowns
- **Bug fix**: Toast said "[G to talk]" but actual key is Enter — changed to "[Enter to talk]"
- **Bug fix**: `getGameHour` was hardcoded to `12` in both NPC approach controller and ambient conversation manager — wired to `gameTimeManager.hour` so time-of-day affects approach probability and greetings
- **Feature**: Wired `getEnvironment` callback so context-aware greetings now fire based on weather (via `weatherSystem.weatherType`) and time period (via `gameTimeManager.timeOfDay`)
- Added 23 audit tests covering: end-to-end approach flow, personality-driven probability, context-aware greetings, frequency tuning/cooldowns, guard conditions, event emission, NPC lifecycle
- Files changed:
  - `shared/game-engine/rendering/BabylonGame.ts` — fixed "[G to talk]" → "[Enter to talk]", wired getGameHour to gameTimeManager.hour (2 places), added getEnvironment callback with weather + time
  - `shared/__tests__/NPCConversationInitiationAudit.test.ts` — new file (23 tests)
- **Audit findings summary:**
  - NPCInitiatedConversationController is well-designed and fully functional: personality-driven probability, proximity detection, greeting generation, cooldowns, and event emission all work correctly
  - Three bugs fixed: keybinding mismatch, hardcoded game hour (×2), missing environment context
  - Remaining TODOs: `hasActiveQuestForPlayer` and `playerIsNew` in getEnvironment are stubbed to false — need wiring to quest system and playthrough state
  - AmbientConversationSystem (NPC-to-NPC conversations) is initialized and paused during player conversations
- **Learnings for future iterations:**
  - Tests in `shared/` must go under `shared/__tests__/` (not `shared/game-engine/rendering/__tests__/`) due to vitest include pattern
  - gameTimeManager exposes `.hour` (0-23) and `.timeOfDay` ('dawn'|'morning'|'midday'|'afternoon'|'evening'|'night')
  - weatherSystem exposes `.weatherType` ('clear'|'cloudy'|'overcast'|'rain'|'storm')
  - 828 pre-existing typecheck errors in the codebase — verify only your changed files
---

## 2026-04-01 - Audit: Action Hotspots
- **Audit finding**: ActionHotspotGenerator (shared/game-engine/logic/ActionHotspotGenerator.ts) was fully implemented with 14 hotspot rules and vocabulary data, but NEVER called — completely dead code
- **Interior hotspots**: Already working — BabylonGame.ts registers action hotspots on interior furniture via PlayerActionSystem.getHotspotsForBusiness() when player enters buildings (line 9435)
- **Outdoor hotspots**: Missing entirely — no fishing/mining/herbalism/chopping spots generated in the open world
- **Fix**: Created ActionHotspotIntegration module (shared/game-engine/logic/ActionHotspotIntegration.ts) that:
  - Collects WorldLocation[] from buildingData (buildings with businessType/position)
  - Clusters nature meshes (trees→forest, rocks→quarry) to avoid per-tree hotspots
  - Calls ActionHotspotGenerator.generate() and creates visible marker meshes
  - Registers markers with InteractionPromptSystem for player interaction
- Wired into BabylonGame.ts generateProceduralWorld() after nature post-spawn cleanup
- Skips interior-only action types (cooking, crafting, reading, praying, sweeping, painting) to avoid duplicates
- Added cleanup in disposeWorld() for outdoor hotspot meshes
- Files changed:
  - `shared/game-engine/logic/ActionHotspotIntegration.ts` — new module (collectBuildingLocations, collectNatureLocations, generateWorldHotspots, getHotspotPromptText)
  - `shared/game-engine/rendering/BabylonGame.ts` — import integration module, generate outdoor hotspots after nature, dispose hotspot meshes
  - `shared/__tests__/action-hotspot-integration.test.ts` — 19 tests covering building collection, nature clustering, e2e generation, prompt text
- **Event pipeline verified**: physical_action_completed → QuestCompletionEngine.trackPhysicalAction() already wired (BabylonGame.ts line 3743)
- **Learnings for future iterations:**
  - ProceduralNatureGenerator exposes getTreeMeshes() and getRockMeshes() for accessing generated nature elements
  - buildingData Map stores position, metadata (businessType, buildingType, etc.) for all placed buildings
  - Nature clustering with 15m radius groups nearby trees/rocks to generate 1 hotspot per grove/outcrop instead of per-mesh
  - Interior hotspot types (cooking, crafting, etc.) are registered separately via furniture matching — don't duplicate outdoors
  - Vitest include patterns: shared/__tests__/**/*.test.ts — tests must be placed there to be discovered
---

## 2026-04-01 - Audit: Listen-and-Repeat System
- **Critical bug found and fixed**: BabylonGame.ts `setOnListenAndRepeat` callback emitted `utterance_evaluated` and `action_executed` events but NOT `listen_and_repeat_passed`, which is the event QuestCompletionEngine handles for listen_and_repeat objectives. Quest objectives could never complete.
- **Fix**: Added `listen_and_repeat_passed` event emission in BabylonGame.ts callback
- **Duplicate event fix**: Controller's individual events (`utterance_evaluated`, `pronunciation_attempt`, `action_executed`) were being forwarded through BabylonChatPanel's `emitEvent` callback AND the final RepeatAttemptResult was also sent through the same `onListenAndRepeat` callback, causing duplicate event emission. Fixed by filtering out individual controller events in BabylonGame callback (they have `.type` fields matching known event types)
- **Broken test import fix**: `listen-and-repeat-action.test.ts` imported from non-existent `../../client/src/components/3DGame/actions/ListenAndRepeatAction` — fixed to `../game-engine/logic/actions/ListenAndRepeatAction`
- **Updated controller test expectations**: Tests expected 2 emitEvent calls but controller emits 3 (includes `pronunciation_attempt`); updated to 3
- **Created E2E integration test**: `shared/__tests__/listen-and-repeat-e2e.test.ts` — 15 tests covering full pipeline from controller through BabylonGame callback to QCE objective completion
- Files changed:
  - `shared/game-engine/rendering/BabylonGame.ts` — fixed setOnListenAndRepeat callback: skip individual controller events, emit listen_and_repeat_passed
  - `shared/__tests__/listen-and-repeat-action.test.ts` — fixed import path
  - `shared/__tests__/listen-and-repeat-controller.test.ts` — updated emitEvent call count expectations
  - `shared/__tests__/listen-and-repeat-e2e.test.ts` — new E2E integration test
- **Learnings for future iterations:**
  - ListenAndRepeatController.emitEvents() fires 3 events (utterance_evaluated, pronunciation_attempt, action_executed) via the emitEvent callback, AND BabylonChatPanel also passes the RepeatAttemptResult through the same onListenAndRepeat callback — always check for duplicate routing
  - QCE event types must match exactly — `listen_and_repeat_passed` is the only type that triggers trackListenAndRepeat()
  - BabylonChatPanel.ts lines 637-681: controller instantiation with TTS/STT/notification callbacks
  - BabylonGame.ts line 2870: setOnListenAndRepeat wiring
  - QCE line 449: listen_and_repeat_passed handler; line 646: trackListenAndRepeat method (requires `passed=true`, default requiredCount=3)
## 2026-04-01 - Crafting System E2E Audit & Fix
- **Problem**: RecipeCraftingSystem was fully implemented but never wired into the game — players could not actually craft items
- **Root causes found**:
  1. `language-learning` genre had `crafting: false` and `inventory: false`
  2. RecipeCraftingSystem was never instantiated in BabylonGame (only resource-based CraftingSystem was)
  3. No crafting station detection or interaction handling existed
  4. No `crafting_station` type in InteractionPromptSystem or ContextualActionResolver
- **Fixes applied**:
  - Enabled `crafting: true` + `inventory: true` for language-learning genre
  - Wired RecipeCraftingSystem into BabylonGame: initialization, callbacks, update loop, dispose
  - Added `getInventoryCountForIngredient()` to RecipeCraftingSystem for UI querying
  - Extended getCraftingRecipes/onCraftItem to include recipe-based crafting alongside resource-based
  - Added `crafting_station` to InteractableType + FurnitureInteractionType
  - Added craftingStationMeshes registration/detection in InteractionPromptSystem
  - Added `resolveCraftingStationActions()` in ContextualActionResolver with French labels
  - Added `handleOpenCraftingPanel()` in BabylonGame — shows recipe menu via contextual action system
  - Auto-detects crafting stations in building interiors via mesh name and building type
- Files changed:
  - `shared/game-genres/index.ts` — enabled crafting+inventory for language-learning
  - `shared/game-engine/logic/RecipeCraftingSystem.ts` — added getInventoryCountForIngredient()
  - `shared/game-engine/rendering/BabylonGame.ts` — import + field + init + update + dispose + station detection + crafting panel handler
  - `shared/game-engine/rendering/InteractionPromptSystem.ts` — crafting_station type + mesh registry + detection
  - `shared/game-engine/rendering/actions/ContextualActionResolver.ts` — crafting station action resolver
  - `shared/__tests__/crafting-e2e.test.ts` — 30 E2E tests covering full pipeline
- **Learnings for future iterations:**
  - `canCraft()` has a difficulty gate: recipe.difficulty must be <= skill.level + 2 — easy to miss when testing
  - RecipeCraftingSystem uses callback pattern (not direct inventory reference) for loose coupling
  - Building interior furniture registration happens in the enter-building handler around line ~9450 of BabylonGame
  - The ContextualActionResolver switch is the single source of truth for action resolution per InteractableType
  - Pre-existing typecheck errors in BabylonGame are widespread (playthroughMeta, DataSource types) — unrelated to changes
---

## 2026-04-01 - Audit: Watch/Observe Quests
- Audited the full observe/watch quest pipeline from NPCActivityLabelSystem → ActivityObservationRewards → QuestCompletionEngine
- **Bug 1 Fixed**: Objective type normalization — aliases like `watch_npc`, `observe`, `watch_activity`, `overhear`, `overhear_conversation` were defined in quest-objective-types.ts but never applied in QuestCompletionEngine. Added normalization in `addQuest()` so all aliases resolve to canonical types.
- **Bug 2 Fixed**: Eavesdrop event not emitted — `endEavesdrop()` in BabylonGame.ts only awarded XP via gamificationTracker but never emitted an `eavesdrop` CompletionEvent. Added event emission to QCE so eavesdrop quest objectives can complete.
- **Bug 3 Fixed**: ActivityObservationRewards not wired — the class existed but was never instantiated in BabylonGame.ts. Initialized it with vocabulary teaching and toast callbacks, wired into the `onActivityObserved` callback alongside the existing QCE tracking.
- Files changed:
  - `shared/game-engine/logic/QuestCompletionEngine.ts` — import normalizeObjectiveType, normalize in addQuest()
  - `shared/game-engine/rendering/BabylonGame.ts` — import + init ActivityObservationRewards, emit eavesdrop event in endEavesdrop(), cleanup in dispose
  - `shared/__tests__/observe-watch-audit.test.ts` — new test file (17 tests)
- **Learnings for future iterations:**
  - QCE is fed events directly (not via GameEventBus subscription) — always use `engine.trackEvent()` for quest tracking
  - `normalizeObjectiveType()` from quest-objective-types.ts is the single source of truth for type aliases
  - ActivityObservationRewards emits to GameEventBus (for other systems) but QCE needs direct `trackEvent()` calls
  - The eavesdrop loop doesn't extract topics from conversation content — defaults to 'general' topic
  - Pre-existing typecheck error: BabylonGame.ts line 13172 has `import('./QuestCompletionEngine')` that should be `import('../logic/QuestCompletionEngine')` (unrelated)
---

## 2026-04-01 - Audit: Journal and Case Notes with Main Quest Progress
- Audited the full journal/case notes/clue pipeline: ClueStore, MainQuestProgressionManager, GameMenuSystem, BabylonGame wiring
- **Gaps found and fixed:**
  1. **No clue discovery notification** — Added `clue_discovered` event listener in BabylonGame.initClueStore() that shows a "New Clue Discovered!" toast with progress count
  2. **Chapter gating never wired** — ClueStore.setAllowedChapters() existed but was never called; added syncClueStoreChapterGating() method called after every fetchMainQuestJournalData() to sync active/completed chapter IDs
  3. **Eavesdrop not connected to ClueStore** — addEavesdropClue() existed but was never called; wired npc_conversation_turn events to clueStore.addEavesdropClue()
  4. **ClueStore not in save/load pipeline** — Clues were lost between sessions; added clueState to GameSaveState, GameStateSource/Target interfaces, SUBSYSTEM_KEYS, captureState/applyState, SaveConflictResolver subsystem list, and BabylonGame source/target implementations
- **Existing working parts verified:**
  - Case notes ARE generated on quest progress (MainQuestProgressionManager.recordQuestCompletion calls addCaseNote)
  - Journal tab in GameMenuSystem renders case notes with categories and day numbers
  - Clues tab renders evidence board with category filters, connections, and chapter grouping
  - fetchMainQuestJournalData IS called after quest completions (line 14038) and CEFR unlocks (line 4016)
  - ClueStore correctly auto-discovers clues from text_collected, conversation_turn, photo_taken, location_visited, investigation_completed, location_discovered events
- Files changed:
  - `shared/game-engine/rendering/BabylonGame.ts` — clue_discovered toast, eavesdrop→ClueStore wiring, syncClueStoreChapterGating, save/load integration
  - `shared/game-engine/types.ts` — added clueState to GameSaveState
  - `client/src/components/3DGame/WorldStateManager.ts` — getClueState/restoreClueState in interfaces, SUBSYSTEM_KEYS, captureState, applyState, computeDiff
  - `shared/game-engine/logic/SaveConflictResolver.ts` — added clueState to subsystem merge list
  - `shared/game-engine/drift-manifest.json` — updated hash for types.ts
  - `shared/__tests__/journal-case-notes-integration.test.ts` — 18 new tests
- **Learnings for future iterations:**
  - ClueStore chapter gating is opt-in: if setAllowedChapters is never called (empty set), all clues pass through
  - Case notes are stored server-side in main_quest_state truth (via MainQuestProgressionManager.saveMainQuestState), not in GameSaveState
  - ClueStore state is purely client-side/in-memory — needs GameSaveState for persistence
  - BabylonGame's journal data is fetched from server and cached in mainQuestJournalData — it's not a live view
  - Existing ClueStore.test.ts at shared/game-engine/rendering/__tests__/ is NOT in the vitest include glob — tests there don't run
  - The `npc_conversation_turn` event only has npcId and topicTag, not the second NPC's identity
---

## 2026-04-03 - Audit: Building Entry/Exit and Interior Interaction
- Created comprehensive audit test suite: `shared/__tests__/BuildingEntryExitAudit.test.ts` (57 tests)
- **Audit scope**: BuildingEntrySystem (entry/exit flow, door proximity, transitions) and InteriorNPCManager (NPC selection, placement, business hours, furniture roles)
- **Verified**:
  1. Building registration calculates correct door positions (front face center, rotated)
  2. Enter/exit flow: teleports player, saves/restores overworld position, plays door sounds, fires callbacks, shows toast
  3. Double-entry prevention (no-op when already inside)
  4. NPC pause/resume on building entry/exit (overworld NPCs freeze during interior visit)
  5. InteriorNPCManager integration: populates NPCs on entry, clears on exit
  6. Business hours: all 8 types (Bakery, Bar, Restaurant, Shop, GroceryStore, Hospital, Church, School) validated
  7. Overnight business hours (Bar 16-2) work correctly
  8. 24/7 businesses (Hospital) work correctly
  9. Employee shift filtering (day/night) works — day shift employees excluded at night
  10. Residence occupant placement (from metadata + residenceId fallback scan)
  11. MAX_INTERIOR_NPCS cap (6) enforced
  12. Furniture role assignment per business type with persistent assignments across visits
  13. Greeting system triggers from owner/employee, uses business-specific messages
  14. Animation state resolution (work animations → 'work', standard states pass through)
  15. Schedule-based NPC updates: employees removed when business closes, NPCs enter/exit per schedule
  16. Full E2E cycle for all 8 business types + residence
- **No bugs found** — the building entry/exit and interior interaction systems are well-implemented
- Files changed:
  - `shared/__tests__/BuildingEntryExitAudit.test.ts` — new audit test file (57 tests)
- **Learnings for future iterations:**
  - BuildingEntrySystem has `_transitionOverride` hook for testing — skip fade transitions
  - Babylon.js MeshBuilder/StandardMaterial/DynamicTexture must be mocked via `vi.mock()` for unit tests (deep engine deps)
  - `@babylonjs/gui` (AdvancedDynamicTexture, Rectangle, TextBlock) needs separate mock from `@babylonjs/core`
  - Bar is the only overnight business (open 16, close 2) — no single hour has all businesses open simultaneously
  - Employee shift filtering at line 619 defaults to 'day' shift when shift prop is absent
  - Off-shift employees can still appear as patrons/visitors via random selection in `findCandidateNPCs`
  - `window.addEventListener` must be stubbed in vitest since BuildingEntrySystem binds keyboard in constructor

## 2026-04-03 - Audit: Assessment System — Can the Initial Language Assessment Be Completed?
- **Audit findings**: The initial language assessment (arrival encounter) CAN complete end-to-end, but had 3 bugs affecting quality:
  1. **Conversation score never passed through event** — BabylonGame emitted `assessment_conversation_completed` with only `npcId`, no `score` field. AssessmentEngine fell back to `Math.round(maxScore * (0.35 + Math.random() * 0.35))` — random estimation instead of real conversation metrics
  2. **Dimension scores used random jitter** — `Math.random() - 0.5` added to each dimension (vocabulary, grammar, fluency, comprehension, pronunciation) making results non-reproducible across identical inputs
  3. **storeCefrLevel not awaited** — Fire-and-forget call in OnboardingLauncher's onCompleted callback meant CEFR level might not persist before game continues
- **Fixes applied**:
  - `shared/game-engine/rendering/BabylonGame.ts` — Compute conversation score from `result.grammarScore` and `result.targetLanguagePercentage` and include in event
  - `shared/game-engine/logic/AssessmentEngine.ts` — Replace random jitter with deterministic dimension scores derived from total percentage
  - `shared/game-engine/rendering/OnboardingLauncher.ts` — Make onCompleted callback async, await storeCefrLevel before calling completeCurrentStep
- **Tests added**: 29 tests in `shared/__tests__/assessment-system-audit.test.ts` covering:
  - Offline content bank completeness (French, Spanish, generic fallback)
  - Reading/writing/listening scoring with rubrics
  - Full 5-phase E2E completion with mock event bus
  - Conversation score passthrough (not random fallback)
  - Dimension score determinism (3 identical runs produce identical scores)
  - CEFR mapping at all thresholds (A1/A2/B1/B2)
  - OnboardingLauncher helpers (isFirstPlaythrough, isLanguageLearningWorld, getTargetLanguage)
  - Phase completion trigger events
- Files changed:
  - `shared/game-engine/rendering/BabylonGame.ts` — pass conversation score in event
  - `shared/game-engine/logic/AssessmentEngine.ts` — deterministic dimension scores
  - `shared/game-engine/rendering/OnboardingLauncher.ts` — await storeCefrLevel
  - `shared/__tests__/assessment-system-audit.test.ts` — new audit test file (29 tests)
- **Learnings for future iterations:**
  - AssessmentEngine (shared/game-engine/logic/) is the in-game orchestrator; client/assessment/AssessmentEngine.ts is a separate formal state-machine version
  - Offline content bank has French + Spanish + generic fallback; other languages fall back to generic English
  - Conversation scoring has two paths: (1) offline heuristic via onConversationSummary metrics, (2) LLM via AssessmentConversationController EVAL blocks
  - BabylonGame emits `assessment_conversation_completed` from two places: onConversationSummary (with score) and handleConversationEnd (safety net, no real score)
  - The `_assessmentTargetNpcId` is set by `assessment_conversation_quest_start` handler — depends on NPCs existing in the world
  - Assessment session is NOT persisted server-side (no POST /api/assessments call) — only CEFR level is stored via storeCefrLevel
---

## 2026-04-05 - US-001: Wire auto-level-up with UI celebration
- **What was implemented**: Wired `checkCEFRAdvancement()` to automatically run after every conversation with meaningful progress (after `calculateFluencyGain()`). When thresholds are met, the player's CEFR level advances with a celebration toast and Prolog fact assertion.
- **Key changes**:
  - `LanguageProgressTracker.endConversation()` now calls `checkAndAdvanceCEFR()` after fluency gain calculation
  - `checkAndAdvanceCEFR()` enhanced with safeguards: no demotion (nextLevel must be strictly higher), 30-min minimum between advancements, 3 conversations minimum between advancements
  - Added `_lastAdvancementTimestamp` and `_conversationsSinceLastAdvancement` fields, persisted in export/import
  - `BabylonGame.ts` wires `onCEFRAdvancement` callback: shows celebration toast with level description from `getCEFRDescription()`, asserts `player_cefr_level(player, <level>)` Prolog fact (matching AssessmentEngine pattern), emits `cefr_level_advanced` event on GameEventBus
  - Added `cefr_level_advanced` event type to `GameEvent` union in `GameEventBus.ts`
- **Files changed**:
  - `shared/game-engine/logic/LanguageProgressTracker.ts` — added safeguard fields, wired checkAndAdvanceCEFR after endConversation, enhanced checkAndAdvanceCEFR with safeguards, persisted new fields in export/import
  - `shared/game-engine/rendering/BabylonGame.ts` — added CEFR advancement callback with celebration toast, Prolog assertion, and event emission
  - `shared/game-engine/logic/GameEventBus.ts` — added `cefr_level_advanced` event type
- **Learnings for future iterations:**
  - `checkAndAdvanceCEFR()` existed on LanguageProgressTracker but was never called — the plumbing was ready, just needed wiring
  - `onCEFRAdvancement` callback was already defined as a field, just needed `setOnCEFRAdvancement()` to be called from BabylonGame
  - CEFR advancement functions take CEFRLevel as parameters (not cached) so they naturally use the latest level
  - `progress.cefrLevel` on LanguageProgress interface needs to be updated alongside `_cefrLevel` private field
  - GameEventBus event types are a union type — adding new events requires adding to the union in GameEventBus.ts
  - Prolog fact pattern: `player_cefr_level(player, <lowercase_level>)` — must match AssessmentEngine._assertAssessmentResult
---
